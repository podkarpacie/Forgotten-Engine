# Forgotten Engine Capability Matrix

This matrix is the source-controlled statement of **current behavior**, not an aspirational feature list. A capability marked **partial** is intentionally not equivalent to a supported gameplay system. Every release must update this document and the paired machine-readable JSON matrix before claiming broader compatibility.

## Status vocabulary

| Status | Meaning |
|---|---|
| `supported` | Implemented, tested, and available through the declared profile contract. |
| `partial` | A bounded foundation exists, but the complete gameplay or protocol behavior is not available. |
| `deferred` | Explicitly planned but not yet implemented. |
| `not-applicable` | Not part of the selected profile’s declared surface. |
| `unknown` | Requires compatibility research before implementation or claim. |

## Profile baseline

| Capability | `fe-7.4` — Tibia 7.4 | `fe-8.0` — Tibia 8.0 | `fe-1.2` — TFS 1.2 / Tibia 10.98 |
|---|---|---|---|
| Native 740 top-level container-to-container stack transfer | partial — a mapped item in one current caller-owned non-nested top-level container may move a requested bounded count into a distinct current caller-owned non-nested top-level container after exact source ID, count, and ownership validation. FE merges only an identical item instance or appends one bounded stack, atomically persists both windows, and advances the existing container refresh epoch. Same-container ordering, nested/closed windows, swaps, ground/depot/inbox/trade, capacity/weight, scripts, and generic inventory behavior remain deferred | deferred | deferred |
| Static due-reactivation retry cadence | partial — when a due inactive static creature cannot return because a player or active static creature occupies its spawn tile, FE records one fresh complete configured interval before attempting that same reactivation again. This clean-room bounded behavior follows the observed TFS failed-spawn retry cadence. Chance-based spawn pools, spectator zones, autonomous scheduler ownership, AI, scripts, and real-client service-lifecycle confirmation remain deferred | deferred | deferred |
| Sandboxed TFS-style `table.create` | partial — fresh no-standard-library Lua expression and explicit callback VMs expose only a VM-local clean-room `table.create(arrayCapacity, recordCapacity)` helper, matching the local TFS registration surface. Both nonnegative capacities are capped at `256`; resulting tables remain local and only existing primitive values may return across FE’s boundary. Filesystem, network, process, package, debug, host APIs, TFS userdata, module loading, automatic registry execution, event-family semantics, and gameplay mutation remain deferred | deferred | deferred |
| Sandboxed TFS-style `table.pack` | partial — fresh no-standard-library Lua expression and explicit callback VMs expose a VM-local clean-room `table.pack(...)` helper, matching the local TFS registration surface. It accepts at most `256` arguments, preserves their sequence and stores the original count in local field `n`, including nil entries. Resulting tables cannot cross FE’s primitive-only boundary. Filesystem, network, process, package, debug, host APIs, TFS userdata, module loading, automatic registry execution, event-family semantics, and gameplay mutation remain deferred | deferred | deferred |
| Sandboxed deterministic Lua math subset | partial — fresh no-standard-library Lua expression and explicit callback VMs expose only VM-local `math.abs`, `math.ceil`, `math.floor`, `math.min`, and `math.max`, reflecting the pure math surface available through TFS’s opened Lua libraries. Variadic extrema require at least one numeric argument and accept at most `256`. Random state, time, trigonometry, logarithms, string/table base-library helpers beyond FE’s separately documented table helpers, filesystem, network, process, package, debug, host APIs, TFS userdata, module loading, automatic registry execution, event-family semantics, and gameplay mutation remain deferred | deferred | deferred |
| Sandboxed ASCII Lua string subset | partial — fresh no-standard-library Lua expression and explicit callback VMs expose only VM-local ASCII `string.len`, `string.lower`, `string.upper`, `string.reverse`, and `string.sub`. Inputs are capped at `1,024` bytes, non-ASCII input is rejected, and `string.sub` implements bounded Lua-style positive and negative byte indices. Pattern matching, binary/Unicode semantics, formatting, byte/character conversion, repetition, global string state, filesystem, network, process, package, debug, host APIs, TFS userdata, module loading, automatic registry execution, event-family semantics, and gameplay mutation remain deferred | deferred | deferred |
| Native 740 empty channel-list acknowledgement | partial — FE decodes only the exact zero-payload classic Request Channels (`0x97`) action and replies with the parser-backed zero-count Channels (`0xAB, 0x00`) record. Protocol and authenticated socket regressions verify exact framing and later session usability. Channel definitions, joining, leaving, membership, public/private channel messages, moderation, persistence, guild/party integration, scripts, and real-client confirmation remain deferred | deferred | deferred |
| TFS public channel catalog and native list | partial — FE optionally loads `data/chatchannels/chatchannels.xml`, accepts only bounded empty `<channel>` entries under one `<channels>` root, and retains only unique nonzero `id`, bounded `name`, and `public="true"` definitions. Script attributes are parsed only as ignored metadata; no script path is resolved or executed. Native 740 Request Channels serializes the deterministic ID-ordered public catalog through the parser-backed `0xAB` list. Non-public channels, channel joins/leaves, membership, messages, private/guild/party channels, moderation, persistence, scripts, and real-client confirmation remain deferred | deferred | deferred |
| New-world public channel catalog skeleton | partial — FE creates an empty standard `data/chatchannels/chatchannels.xml` `<channels>` root with new content skeletons, allowing an operator to populate a compatible public catalog later. Reconciliation preserves any operator replacement instead of overwriting it. Default gameplay channels, scripts, channel membership, messages, moderation, persistence, and real-client confirmation remain deferred | deferred | deferred |
| Native 740 configured public-channel open | partial — FE decodes the exact classic two-byte Join Channel (`0x98`) request and opens only a matching validated public catalog entry with parser-backed Open Channel (`0xAC`) fields for ID, name, zero players, and zero invites. Unknown, non-public, or absent catalog IDs produce no response. FE does not retain membership, authorize channel chat, deliver messages, close channels, expose private/guild/party channels, support moderation or persistence, run scripts, or claim real-client confirmation | deferred | deferred |
| Native 740 configured public-channel message | partial — FE retains inbound Talk mode and optional classic channel ID. Only classic mode `7` for a configured public channel opened in that same live session is accepted. FE normalizes and bounds the text, fans it out through the existing bounded shared chat queue, filters delivery by each session’s local opened-channel IDs, and encodes the classic `0xAA` mode-7 Talk record with its channel ID. No persistent membership exists. Leave handling, channel permissions, private/guild/party channels, non-mode-7 channel variants, message history, moderation, flood limits, scripts, and real-client confirmation remain deferred | deferred | deferred |
| Native 740 bounded private message | partial — FE retains classic mode `5` recipient input and routes a normalized bounded text event only to an exact case-sensitive live session-registered character name. The recipient alone receives the parser-shaped `0xAA` mode-4 (`MessagePrivateFrom`) Talk record with the sender name and text. Offline targets, sender acknowledgements, account lookup, contact lists, persistence, ignore/ghost/level/premium policies, moderation, private-channel setup, red-private variants, scripts, and real-client confirmation remain deferred | deferred | deferred |
| Native 740 configured public-channel leave | partial — FE decodes exact classic Leave Channel (`0x99`) with one 16-bit ID and removes that ID only from the current session’s local configured public-channel set. The request emits no frame, mirroring the bounded local state-removal behavior. Persistent membership, a server close record, permissions, history, moderation, private/guild/party behavior, scripts, and real-client confirmation remain deferred | deferred | deferred |
| Account-owned VIP persistence and native action foundation | partial — SQLite schema v17 stores bounded account-owned VIP entries by exact persisted character name/ID with description capped at `128` bytes, 32-bit icon, and notify flag. CRUD validates account ownership, target existence, duplicates, missing entries, and deterministic target-name ordering. For classic 740, FE decodes exact Add VIP (`0xDC`), Remove VIP (`0xDD`), and Edit VIP (`0xDE`) requests against the authenticated account. A successful add emits the local parser’s no-additional-info `0xD2` record. After world bootstrap, login emits the deterministic first `128` persisted entries, showing a target online only when it is already active in the shared native world. Each active session registers a bounded `64`-event queue for only those delivered valid target IDs. A matching target’s later login enqueues parser-verified classic `0xD3 + id` online status, and its disconnect enqueues `0xD4 + id` offline status; slow or disconnected watchers cannot block other session lifecycle changes. Notifications, group quotas, client UI confirmation, ignore/privacy policy, presence persistence, Lua, guild/party integration, non-740 layouts, and real-client confirmation remain deferred | deferred | deferred |
| Guild persistence foundation | partial — SQLite schema v18 atomically creates a validated guild with one persisted owner, TFS-style `the Leader`/`a Vice-Leader`/`a Member` ranks at levels 3/2/1, and one leader membership. Guild names and custom rank names are capped at `64` bytes; MOTD is capped at `255` bytes; the membership primary key allows only one guild per player. Transactional storage APIs add an existing player to an existing guild at its level-1 member rank, reject a second membership, assign an existing member to only a same-guild rank, remove a non-owner member, replace an existing guild MOTD, and replace an existing member nick within its `15`-byte bound. A guild may have at most `20` ranks total; FE creates, renames, and deletes only custom ranks with unique same-guild names and levels. Required levels 1/2/3 cannot be deleted, and an assigned rank cannot be deleted. A transfer only accepts an existing member, atomically promotes that member to level 3, demotes the former owner to level 2, preserves member nicknames, and updates the unique owner identity. Schema v19 stores TFS-shaped unique `(player_id, guild_id)` pending invitations, caps each guild at `20`, rejects existing members/duplicates, lists them deterministically, and revokes one exact relation. An exact pending invitation may be atomically accepted into the required level-1 member rank, then all competing invitations for that player are removed. A deletion transaction removes the guild plus all FE-owned pending invitations, memberships, and ranks in dependency order, releasing its name and owner identity for later reuse. Deterministic rank, membership, MOTD, nickname, transfer, invitation, and deletion success/failure paths are covered. Native guild packets, authorization, rank permissions, wars, bank features, Lua, houses, party integration, and real-client confirmation remain deferred | deferred | deferred |
| Session-local party relationship foundation | partial — the authoritative live world records only party leaders, member-to-leader relationships, and invitation sets. It validates active players, rejects duplicate invitations and multiple memberships, clears competing invitations on acceptance, deterministically transfers leadership to the lowest live member when a leader leaves, and cleans membership/invitations on player removal. A current leader can also transfer ownership only to an existing live member; FE atomically rewrites every member relationship, makes the prior leader a member, and rekeys outstanding invitations. Classic 740 strictly decodes Invite (`0xA3`), Join (`0xA4`), Revoke Invitation (`0xA5`), Pass Leadership (`0xA6`), and Leave (`0xA7`) request records and routes valid active-player IDs to that same synchronized core without ending either session on rejected IDs or invalid transitions. A strict profile-gated encoder produces classic creature-party (`0x91`) records for only the audited none, invitation, leader, and member values (`0`–`4`) and rejects non-player native IDs. A dedicated shared-world party epoch advances after successful party mutations and player removal; active native sessions issue a detached current shield snapshot on the next loop before their following action. Core, protocol, and two-session socket regressions cover display relations, leadership transfer, invitation continuity, exact request layouts, leader-side live shield delivery, revoke-before-join rejection, membership creation, leave cleanup, shield golden bytes, and subsequent ping usability. Shield refreshes address the current all-active native render set, not a TFS spectator culling policy or complete party UI state. Shared experience, private party channels, loot policy, combat effects, Lua hooks, moderation, persistence, cross-profile behavior, and real-client confirmation remain deferred | deferred | deferred |
| Native 740 container item slot-type guard | partial — the bounded catalog-mapped owned non-nested top-level-container-to-empty-fixed-equipment `ThrowItem` route applies the same validated nonempty legacy `items.xml` `slotType` rule as the bounded map-source route before mutating authoritative state. Exact direct mappings are admitted, known mismatches and two-handed placement are rejected, and absent metadata preserves the prior route. Occupied-slot exchanges and merges, stacks, capacity, nesting, generic inventory semantics, full TFS bitmasks, and real-client confirmation remain deferred | deferred | deferred |
| Profile selection/configuration | supported | supported | supported |
| General frame/status service foundation | supported | supported | supported |
| Deterministic multithreaded render-publication foundation | partial — a bounded 1–8 worker pool accepts only detached immutable native 740 viewport publications. Every worker has a 32-request queue, one ordered batch caps at 256 publications, caller-provided sequence numbers are sorted before scheduling, duplicate sequences are rejected, and completed frames are returned in the same sequence regardless of worker completion order. The pool has no `SharedNativeWorld`, database, socket, action queue, or mutation callback. A fixed three-publication release benchmark measured the pool 8.633% slower than direct preparation, so it remains outside the live listener and makes no throughput or scalability claim. Authoritative command application, listener integration, queue telemetry, gameplay workers, live lock/latency/memory measurements, and real-client confirmation remain deferred | deferred | deferred |
| Native unmodified OTClientV8 game path | partial — experimental 740 login, character selection, map, movement, shared-player visibility, bounded player-vitals updates, and a parser-shaped zero-count Quest Log acknowledgement. Active peers render the shared authoritative classic outfit, direction, and bounded health percentage. An actual shared-vital change triggers the existing complete viewport refresh after each session’s parser-shaped self stats update. Accepted turns and moves advance the shared visibility epoch and re-render visible peer sessions through the same full-map path. A native socket regression proves the Quest Log response leaves the session usable for a following outfit-window request. The 740 player-stats record has parser-shaped 16-bit level and soul fields, but unmodified-client confirmation remains required | partial — explicit profile foundation recognizes that classic 8.0 requires RSA/XTEA transport, includes a bounded outbound-only opaque XTEA envelope primitive, and can extract only the marker plus four XTEA words from an already decrypted fixed block for future parser-backed work. Native enablement remains refused: there is no client-input decryption, credential/login/game parser, listener, packet-layout claim, or client interoperability claim | deferred |
| Native 740 combat target cancellation | partial — parser-backed zero-payload `0xBE` clears only the authoritative target and follow intents for the current authenticated player. A rejected nonzero `SelectTarget` request receives the parser-verified zero-payload classic `ClearTarget` (`0xA3`) record; accepted targets and manual target clear produce no extra feedback. Native selection and follow now reject an authoritative player marked dead before mutating the source intent. When FE’s existing bounded selected-static melee transition actually deactivates that same selected target, the initiating session receives one `ClearTarget` record before the existing zero-health and visibility delivery; a nonlethal hit receives none. These controls add no generic attack, target selection policy, movement change, client effect, or generic combat output. Protocol, core, host, and live socket regressions verify strict packet shapes, deterministic intent clearing, unavailable-target rejection, rejected-target feedback, and deactivation-only control delivery. All combat formulas, effects, broad target rules, cross-player cleanup, loot, PvP, and client confirmation remain deferred | deferred | deferred |
| Native 740 explicit attack-target clear | partial — the parser-backed classic `ClientAttack` request (`0xA1`) carries one 32-bit creature ID. A zero ID now clears only the current player’s bounded authoritative target and emits the verified zero-payload `ClearTarget` (`0xA3`) record, including when no target was retained. This mirrors the bounded upstream attack-clear response without borrowing its code. A live socket regression verifies the full zero-ID request and response. The route does not add an attack, formula, timing, authorization, movement, effect, loot, PvP, script, or generic combat behavior | deferred | deferred |
| Native inspection requests | partial — parser-backed `LookMap` (`0x8c`) validates a uniquely catalog-mapped exact authoritative map item and returns the verified classic `0xb4` status-text record with message class `0x15`. The base text exposes the server ID and count; when that exact source item has imported legacy name and weight metadata, FE appends bounded `Name:` and stack-aware hundredths-of-an-ounce details before any size-bounded imported map description. Parser-backed `LookCreature` returns the same status record only for a known active static creature or known player on the same floor inside the exact current 18×14 classic viewport; it exposes only the validated name. Missing, inactive, cross-floor, or off-screen targets produce no packet. TFS articles, pluralization, attributes, recursive container weight, capacity, health/level, and all unverified inspection variants remain deferred | deferred | deferred |
| Native 740 fixed-equipment LookMap | partial — ordinary native `LookMap` additionally accepts only `x = 0xFFFF`, one unflagged fixed equipment-slot code, `z = 0`, zero stack position, and an exact validated catalog client item ID that matches the caller’s current authoritative slot item. It returns the existing parser-shaped status text with slot code, server item ID, count, and optional bounded imported legacy name and weight details. Container positions, nonzero stacks, map items beyond the existing route, TFS articles/pluralization, descriptions, attributes, and generic inspection remain deferred | deferred | deferred |
| Native 740 top-level container LookMap | partial — ordinary native `LookMap` additionally accepts one current open caller-owned non-nested top-level container item only through its classic flagged `x = 0xFFFF` address, current window ID, exact item index, zero stack position, and exact catalog client item match. It returns the existing parser-shaped status text with container ID, server item ID, count, and optional bounded imported legacy name plus stack-aware weight details. Closed windows, nested containers, absent indexes, mutation, TFS articles/pluralization, descriptions, attributes, and generic inspection remain deferred | deferred | deferred |
| Complete official-client protocol emulation | deferred | deferred | deferred |
| TFS-style `config.lua` discovery | partial — bounded assignment subset and safe defaults | partial — bounded assignment subset and safe defaults | partial — bounded assignment subset and safe defaults |
| OTBM map and companion discovery | partial — map, towns, houses, spawns, and legacy item metadata import boundary | partial — content audit only until profile mapping is verified | partial — content audit only until profile mapping is verified |
| Item type metadata | partial — load/validate map-facing legacy metadata plus bounded operator-supplied `items.xml` armor, weight, slot-type labels, defense, extra-defense, attack-speed, and verified weapon-type labels for known OTB items. Optional legacy unsigned weight values and finite slot-type sets are retained by authoritative server ID as import-only metadata. Validated positive armor values now form immutable server-ID input for the explicit native armor-only bridge; no parsed weight, slot type, defense, extra-defense, attack-speed, or weapon-type field enforces capacity, equipment placement, or other runtime behavior. Full formula, capacity, and placement use remain deferred until profile-specific inventory, container, vocation, and equipment contracts are implemented | deferred | deferred |
| Native ground-item ownership foundation | partial — a synchronized host-owned map wrapper retains an immutable source map plus a mutable runtime map, replaces complete ordered tile-item lists after core validation, advances a revision, and returns detached immutable render snapshots. Direct and cross-thread regressions prove later replacement does not alter an already acquired snapshot. Whole-tile replacement invalidates the affected source bindings rather than guessing new identities. Native startup loads the durable journal, validates every identity against the current source revision before constructing the owner, and fails before the listener starts when recovery is incompatible. Every accepted session receives an owner-derived detached snapshot, and the heartbeat obtains a fresh detached snapshot per tick. One bounded native source-map-to-equipment route now uses this owner and sends a full map refresh after success; compact deltas and broader gameplay item transfer remain deferred | deferred | deferred |
| Source-map revision foundation | partial — FE derives a deterministic non-cryptographic 64-bit revision from complete immutable map source content: map metadata, ordered tiles, ordered/nested map items, flags, houses, towns, and waypoints. A direct regression proves cloned content retains the same revision and changed item count/nested content changes it. The revision now guards the bounded removal journal and source-item pickup foundation; map-content-change reconciliation and native client transfers remain deferred | deferred | deferred |
| Source-bound map-item identities | partial — one existing top-level source item can be identified by the deterministic source-map revision, exact tile position, and preserved ordered item index. Duplicate top-level items remain distinct by index, nonexistent indexes are rejected, and every identity becomes incompatible after any source-content change. The synchronized map retains a runtime-to-source index only until a whole-tile replacement or successful bounded pickup removes that binding. Child identities, reconciliation, and native client ground transfers remain deferred | deferred | deferred |
| Atomic source-map pickup foundation | partial — one direct host call may move an exact plain top-level source-bound map item into one empty authoritative equipment slot. It always locks map then shared player world, rejects source/runtime mismatches and unsupported map attributes, persists the complete player inventory plus the complete revision-bound removal journal in one SQLite transaction, then publishes the map removal and equipment epoch. Success and occupied-slot rejection regressions cover map, memory, SQLite, journal, and epoch state. Native 740 routing now covers one exact catalog-mapped whole-item map source into one empty fixed equipment slot, followed by parser-verified inventory refresh records and a full-map refresh. Container destinations, stack policy, capacity, compact deltas, recovery reconciliation after source changes, and real-client behavior remain deferred | deferred | deferred |
| Native 740 source-map item to equipment | partial — a parsed classic `ThrowItem` (`0x78`) whose source is a normal map coordinate and whose destination is one empty fixed equipment slot reaches the recovered synchronized owner only when the client item ID maps uniquely through the configured catalog and the requested count equals the complete validated source stack. The existing composite transaction persists inventory plus removal journal; FE then emits the existing `SetInventory` record and one full-map (`0x64`) refresh. A live authenticated socket regression verifies map removal persistence, journal identity, inventory delivery, and map delivery. Partial counts, stack merges, occupied slots, containers, capacity, compact map deltas, and real-client confirmation remain deferred | deferred | deferred |
| Native 740 source-map item to container | partial — a parsed classic `ThrowItem` (`0x78`) whose source is a normal map coordinate and whose destination is one open owned non-nested top-level container reaches the recovered synchronized owner only when the client item ID maps uniquely and the requested count equals the complete validated source stack. One composite transaction persists equipment, complete containers, and the removal journal before publishing map and container state. FE emits the existing complete-container refresh plus one full-map (`0x64`) refresh. Direct and live authenticated socket regressions cover the durable container, removal journal, refresh frames, and map removal. Partial counts, stack merges, capacity, nested or closed containers, compact deltas, and real-client confirmation remain deferred | deferred | deferred |
| Imported static direct-melee timing and damage metadata | partial — one parsed monster direct-melee interval and normalized non-negative inclusive damage range survive static spawn materialization into immutable stable-creature-ID metadata. Zero or unknown interval IDs and invalid ranges are rejected. The core converts a valid interval upward to the shared authoritative one-second tick, blocks an already selected active creature before its next due tick, and advances that due tick after its bounded damage transition. When a range exists, each actual static target attack selects the next ascending inclusive value and wraps deterministically; otherwise the configured fixed fallback remains in effect. The shared heartbeat counts a non-due result as a cooldown skip, never as an applied attack. The ascending damage-selection sequence persists through static runtime snapshots and SQLite schema v15. The bounded remaining cooldown delay now persists through schema v16, validates against the imported interval for the installed creature, and rebuilds its due tick relative to a fresh world after restart. Direct regressions cover a `2,000` ms interval, early attempts, exact due-tick release, skip accounting, parser-to-core range propagation, the `2, 3, 4, 2` cycle, restart continuity, and preserved no-metadata behavior. This is a bounded deterministic bridge only: sub-second timing, absolute world tick persistence, TFS randomized selection, formulas, chance, generic monster persistence, AI, scripts, effects, and client behavior remain deferred | deferred | deferred |
| Runtime items, inventory, containers, equipment, depot, inbox | partial — bounded core item/equipment/container primitives, authoritative player equipment/container state, SQLite equipment/container persistence, native-session hydration, and parser-verified catalog-mapped 740 equipment bootstrap in deterministic multi-slot order. Extended diagnostics report unmapped equipped items without exposing packet bodies. A shared authoritative equipment replacement advances a dedicated epoch; a native session compares only catalog-mapped slots already sent at login and emits deterministic parser-verified `SetInventory` or `DeleteInventory` records for a changed, removed, or newly unmapped slot. A parser-verified container-open codec bootstraps top-level persisted containers only when the container and every item have validated mappings; nested or incomplete mappings are omitted. A real shared container replacement advances a dedicated epoch and re-emits only its current complete mapped top-level open-container records. Native 740 `CloseContainer` (`0x87`) is decoded as one client-view container ID and receives the parser-verified `0x6F` close record; that session ID is excluded from later container refreshes without mutating shared or persisted data. Native 740 `UpArrowContainer` (`0x88`) is decoded as exactly one session-view container ID; because FE has no verified parent, nested, or browse-field container model yet, it intentionally produces no packet and changes no shared or persisted state. Native 740 `UpdateContainer` (`0xCA`) requests exactly one complete mapped top-level container for that session, sends the existing parser-verified `0x6E` record when available, and removes that ID from the session’s closed-view set without mutating shared or persisted data. A direct host-only atomic source-map pickup may move one plain revision-bound top-level map item into an empty equipment slot, persist its removal journal with the complete inventory, and advance the existing equipment epoch; the native listener does not route that primitive yet. A native 740 `ThrowItem` (`0x78`) move may transfer one catalog-mapped whole item from a fixed equipment slot into an already-owned, top-level mapped container window. FE validates the exact classic flagged container address, source identity, whole-item count, container ownership, and non-nested destination; it then mutates the shared world, persists equipment and containers, and emits the existing slot-clear plus container-refresh frames. The destination index is a client drop location for that whole-item append. A mapped equipment stack may instead merge a requested partial or full source count into an existing matching item at the exact flagged owned non-nested top-level container index. FE uses its bounded authoritative merge, persists both collections, and emits the resulting `SetInventory` or `DeleteInventory` plus refreshed container record. The reverse move may transfer one catalog-mapped whole item at a flagged owned top-level container index into an empty fixed equipment slot. A requested partial count may instead move from that owned non-nested top-level container item into an empty equipment slot or an existing matching fixed equipment item. FE uses its bounded authoritative merge, persists both collections, and emits `SetInventory` plus the refreshed complete container record. A complete container item may exchange with an occupied fixed equipment slot. A complete fixed-slot source item may now exchange with a distinct occupied fixed-slot target, using cloned authoritative state, SQLite persistence, and the existing inventory refresh epoch. Partial and same-slot exchanges, nonmatching stack destinations, slot compatibility, nested containers, native ground/map transfer routing, depot/inbox, generic item controls, capacity policy beyond core bounds, move events, map delta delivery, and real-client confirmation remain deferred | deferred | deferred |
| Native 740 legacy item slot-type guard | partial — only the bounded catalog-mapped map-source-to-empty-fixed-equipment `ThrowItem` route consults validated nonempty legacy `items.xml` `slotType` metadata. It allows exact direct mappings for head, necklace, backpack, body, right-hand, left-hand, generic hand, legs, feet, ring, and ammo. Known mismatches are rejected before the atomic transfer; absent metadata preserves the earlier route. Two-handed placement, full TFS bitmask semantics, dual-slot atomicity, generic equipment/container transfers, stack behavior, capacity, item actions, and real-client confirmation remain deferred | deferred | deferred |
| Native 740 full container-to-equipment matching-stack merge | partial — a mapped requested source count equal to the entire owned non-nested top-level container item may merge into a fixed equipment destination only when that destination already holds the same mapped item ID. FE persists the resulting empty container and merged equipment stack, then emits the existing `SetInventory` and complete-container refresh records. A live authenticated socket regression verifies a 30-count full-source merge into a 30-count equipment stack. Nonmatching occupied slots, swaps, nested containers, ground/map transfers, capacity policy, generic inventory controls, and real-client confirmation remain deferred | deferred | deferred |
| Map item use | partial — typed server-side validation accepts only same-tile or adjacent use of an exact top-level authoritative map item stack and returns immutable metadata; the same validation is available through synchronized host state. Native 740 `0x82` input is decoded as position, client thing ID, stack position, and index, then reaches that validator only when the configured presentation catalog maps the client thing ID to exactly one server item ID. For one exact validated item with imported teleport-destination metadata, FE performs the sole supported native item action: it rechecks that destination as walkable and unoccupied under the authoritative world lock, performs the distinct explicit player relocation, persists the new position, cancels active click-walk, advances native visibility, and sends the existing full viewport refresh. A blocked destination produces no mutation or packet. Native 740 `UseItemEx` (`0x83`) is decoded as two position/client-thing/stack references and reaches a matching two-target validator only when both IDs map uniquely; both server-owned top-level items must independently pass existing map and range checks. Native 740 battle-window input (`0x84`) is decoded as one source position/client-thing/stack reference plus one creature ID; its source requires a unique mapping and its target must be a live player or active static creature on the same or an adjacent tile. Native 740 rotate-item input (`0x85`) is decoded as one position/client-thing/stack reference and reaches existing exact map-item validation only when its client item ID maps uniquely. Missing or ambiguous mappings, absent maps, and invalid source or target items are rejected without an outbound packet or mutation. All other action execution, rotation, doors, switches, containers, charges, Lua, target selection, combat, messages/effects, and full client item-use behavior remain deferred | deferred | deferred |
| Native 740 read-only text window | partial — after the existing exact `UseItem` validation, an imported non-empty OTBM text value from that same top-level map stack opens one parser-verified `0x96` read-only window. The frame carries a deterministic window ID, the already validated client item ID, byte length, text, and empty writer string only; it omits later writable-date and traded fields. It changes no map, shared, or persisted state. Empty, missing, changed, or mismatched text metadata emits no packet. Editing, saves, writer/date attributes, containers, scripts, descriptions, and generic item actions remain deferred | deferred | deferred |
| Native 740 LookMap imported name, description, and weight | partial — after existing exact validated `LookMap` item resolution, FE may append one bounded legacy `items.xml` name, one imported legacy item weight in hundredths-of-an-ounce form, and one non-empty imported OTBM description from that same top-level stack to the verified `0xB4/0x15` status message. Weight uses source OTB stackability only for the displayed total; missing/mismatched metadata, absent name/weight, empty description, or a frame-oversized addition preserves the preceding bounded text. TFS article/plural forms, attributes, recursive container weight, capacity, text content, writable metadata, and broader inspection behavior remain deferred | deferred | deferred |
| Native 740 stepped-on teleport | partial — after one validated cardinal or diagonal move lands on a tile with exactly one top-level imported teleport destination, FE revalidates that destination as walkable and unoccupied under the authoritative lock. It resolves at most eight direct imported teleport hops, stops safely on a missing, ambiguous, blocked, repeated, or hop-limited target, then persists only the final successful position and sends one full viewport refresh instead of ordinary movement frames. Effects, scripts, generic tile actions, and real-client confirmation remain deferred | deferred | deferred |
| Accounts and character ownership | supported — local SQLite, Argon2 accounts, numeric native account flow, and additive local character creation. `player create <directory> <account-id> <character-name> [vocation-id]` keeps the former account/name form and accepts an optional validated `u16` vocation ID that is durable at insertion; no label-to-vocation inference is claimed. New local characters persist the exact classic cumulative experience threshold for their returned level-eight default, so a later bounded experience award cannot reduce that character’s level. Existing externally created or legacy inconsistent character records are not rewritten automatically | supported — local CLI/persistence foundation | supported — local CLI/persistence foundation |
| Player vitals | partial — persisted health, mana, capacity, and magic level; parser-shaped classic 740 HUD delivery with a 16-bit level and bounded soul byte. A byte-level regression verifies that persisted `42 / 50` mana and a `32,000` capacity retain their distinct classic-740 offsets. A shared-world vocation level-up advances the stats epoch, then the existing native refresh rehydrates authoritative level, experience, and vitals before encoding the frame. A validated native static-creature defeat is now one bounded gameplay experience source: when it crosses a level threshold and the player's vocation has configured gains, the level, experience, current/max health, current/max mana, and capacity snapshot persist atomically. Unmodified-client confirmation remains required | deferred | deferred |
| Player outfit | partial — schema-v11 persists accepted classic-740 look type and color values, then hydrates them after map initialization with a configured look-type fallback for legacy or out-of-range records. A native `0xD2` request receives the classic `0xC8` current-outfit plus one-byte inclusive start/end look-type range. Operators may set `otclientV8OutfitFirstLookType` and `otclientV8OutfitLastLookType`; both must fit the classic byte form and contain the configured current look type, while zero values preserve the current-look default for existing worlds. A concrete `ChangeOutfit` request inside that range persists and echoes the selected appearance; an out-of-range request retains the current stored appearance. On an accepted change, the shared native world records the complete validated classic outfit, advances its visibility epoch, and visible peer sessions re-render that authoritative look type and four colour bytes through their existing full-map refresh. Newer outfit-list, addon, and mount fields are not emitted. Asset validation and unmodified-client confirmation remain deferred | deferred | deferred |
| Death and respawn lifecycle | partial — server-side death/temple respawn plus schema-v9 persisted dead/respawn/loss state and strict native hydration. A configured explicit `deathLosePercent` value from `1` through `100` now applies the bounded exact fixed-percent loss model after supported native player-melee, condition, or static-creature heartbeat deaths, then commits level, experience, vitals, visible skills, exact progression attempts, and marked lifecycle state in one SQLite transaction. `0` preserves no-loss behavior. After the existing authoritative death transition, the native 740 path emits the evidence-backed zero-payload `0x28` death notification and sends no client loss record. Public OTCv8 evidence shows its death dialog performs a safe logout and new character login rather than a revival request. When that next FE native game session authenticates a persisted-dead character, FE invokes the existing authoritative temple-respawn transition before shared-world registration, validates the target tile, atomically persists position/vitals/cleared lifecycle state, and delivers the temple through the ordinary parser-shaped full-map bootstrap. Death-screen timing, automatic timers, direct revive/teleport/loss records, default-loss formula (`-1`), blessing/promotion adjustments, and unmodified-client confirmation remain deferred | deferred | deferred |
| Typed skill progression, experience and vocation rules | partial — typed seven-skill levels/percentages and numeric vocation identity are authoritative, SQLite-persisted, locally provisionable, and delivered in classic 740 bootstrap/refresh records; operator-owned TFS-style `data/XML/vocations.xml` supplies profile-gated online health/mana regeneration, vocation skill multipliers, and parsed `gainhp`, `gainmana`, and `gaincap` metadata. A bounded direct legacy `config.lua` `experienceStages` literal table accepts documented `minlevel`, optional `maxlevel`, and `multiplier` fields without Lua execution. It rejects expressions, duplicate/unknown fields, overlap, invalid values, and excess data; a direct table replaces `rateExp`, while `experienceStages = nil` selects flat `rateExp`. The existing bounded `data/XML/stages.xml` adapter remains a fallback only when the legacy assignment is absent and retains its established `rateExp` composition. All selected sources drive deterministic authoritative operator experience awards with SQLite persistence; existing experience awards and loss resolve levels through a checked integer classic cumulative threshold. When a matching parsed vocation has a positive level increase, the stable CLI and the bounded native static-defeat source atomically persist scaled gains to current/max health, current/max mana, and capacity alongside the new level and experience; absent metadata or no level increase leaves vitals unchanged. The matching shared-world host transition advances the profile-gated stats-refresh epoch and rehydrates the next 740 frame from authoritative level, experience, and vitals. Each successful fixed selected-player melee hit can award one configured fist try with exact persisted counters. Hydrated poison/burning/energy schedules update native vitals; schema-v10 preserves each validated elapsed interval remainder across restart and a fresh native-style relog registration, while client condition-effect packets remain deferred. Lethal bounded condition damage activates the existing persisted server-side death state only at a validated assigned town temple. For configured explicit fixed loss only, the same progression rules now back an atomic post-death loss snapshot; default loss, promotion, and blessing behavior remain unimplemented. Weapon/spell formulas, combat/quest rewards, client death screens/timers/respawn packets, soul, and full TFS-compatible advancement transactions remain deferred | deferred | deferred |
| Vocation combat formula metadata | partial — bounded nested TFS-style vocation `<formula defense="…" armor="…"/>` values are parsed as validated fixed-point metadata, with independent `1.000` defaults when absent. The parsed armor multiplier now scales only FE’s existing capped six-slot armor-only reduction through deterministic integer thousandths. The defense multiplier, fight modes, active blocking, controlled randomness, remaining equipment aggregation, client packets, and wider profile-specific formulas remain deferred | deferred | deferred |
| Vocation timing metadata | partial — bounded TFS-style vocation `attackspeed` and `basespeed` attributes are retained as typed metadata, with verified defaults of 1500 milliseconds and 220 when absent. They do not alter current FE movement, combat cooldowns, packets, or profile compatibility claims; profile-specific timing, item-versus-vocation precedence, and movement formula behavior remain deferred | deferred | deferred |
| Movement and collision | partial — authoritative native player movement, map bounds/walkability/occupancy, bounded click-walk scheduling, SQLite position persistence, and orderly plus abrupt-disconnect native-session relog regressions that restore the saved tile. Unmodified-client confirmation remains required | deferred | deferred |
| Player interaction intent | partial — target/follow selection state with departure cleanup, plus parser-backed native 740 fight-mode, chase-mode, and secure-mode state. Fight preferences use verified attack/chase-off/secure-off defaults and accepted input updates synchronize through authoritative shared state. One parser-verified three-byte `0xA7` player-modes record is appended to native 740 initialization and is sent again only after a real accepted state change; identical repeated input does not create a redundant record. A living current player follow target now permits at most one direct cardinal distance-reducing source step per native heartbeat through authoritative walkability and occupancy checks; a successful batch advances visibility once and persists source positions. Pathfinding, diagonals, retries, facing, attack automation, formations, PvP variants, mitigation, and full TFS follow parity remain deferred | deferred | deferred |
| Native 740 fixed selected-player melee | partial — the existing fallback now constructs a server-owned typed adjacent physical combat event with a one-server-tick interval. This preserves the shared elapsed-second heartbeat cadence, prevents duplicate same-tick damage, applies the current bounded armor-only reduction, and reuses existing persisted damage/death delivery. TFS weapon timing, attack speed, skills, shields, fight modes, random blocking, PvP legality, resistance, effects, automatic attacks, and formula parity remain deferred | deferred | deferred |
| Combat | partial — typed physical adjacent-melee events with bounded damage, deterministic per-player server-tick cooldowns, profile-neutral flat physical mitigation, and an optional scriptless FE declarative weapon catalog. On the current native selected-melee path, only a server-owned matching right-hand item may select a catalog weapon; otherwise no catalog event is created. Separately, an active adjacent selected static target receives fixed 10-percentage-point damage no more than once per authoritative world tick through the same per-player combat cooldown. A real native selected-static hit emits the parser-shaped `CreatureHealth` record immediately, persists the bounded static runtime snapshot, and lets the existing visibility epoch refresh remove a defeated creature. On a real static defeat only, a validated nonzero monster raw-experience value passes through the configured existing rate-and-stage policy. The authoritative award applies configured vocation health, mana, and capacity gains only on a positive level increase, then atomically persists the resulting level, experience, and vitals; the native progression epoch drives existing HUD refresh delivery. Missing vocation metadata or no gained level retains the pre-existing vitals. Loot, corpses, formulas, rewards beyond this source, and AI behavior remain separate. A supported native lethal player-combat transition may now apply and atomically persist the explicit configured fixed death-loss model after validated town/temple state; it sends no additional client loss packet. When the opt-in native static-target policy applies real damage, it persists the resulting vitals for the deterministic affected-player set and persists the existing death state for a lethal valid-temple transition; weapon catalog, mitigation, weapon timing, rewards, client effects, loot, corpses, formulas, scripts, and full AI remain separate. A separate typed spell-cast foundation atomically enforces nonzero bounded mana costs and server-tick cooldowns; an optional declarative spell catalog may construct those events from ID, mana cost, and interval, is retained as immutable native-host configuration, and can be resolved through a synchronized server-only host helper. No spell layer is profile-gated, persistent, or client-routable. TFS Lua weapon scripts, legacy XML formula semantics, map use, client weapon-use packets, armor, shielding, spell names/words/targets/formulas/effects/packets, ranged delivery, resistance, PvP, automatic attacks, and client combat effects remain deferred | deferred | deferred |
| Native 740 armor-only physical mitigation | partial — FE derives the target’s profile-neutral flat physical reduction from positive validated `items.xml` armor values in the authoritative head, neck, armor, legs, feet, and ring slots. The capped sum is multiplied deterministically in thousandths by the validated parsed vocation `armor` multiplier, or the safe `1.000` default when none is available. It is recalculated from current authoritative equipment before each typed declarative selected-player weapon event. The same derived state applies before the existing opt-in fixed static-creature physical attack evaluates damage and death. It is not stored separately; session hydration applies the same derived state. Hands, shields, item defense/extra-defense, the vocation defense multiplier, fight mode, active block, random armor, resistance, fixed fallback player melee, packets, and TFS formula parity remain deferred | deferred | deferred |
| Static creature rendering/occupancy | partial — catalog-derived static spawn representation, native visibility, bounded authoritative 0–100% display health delivered on login and visibility refresh, optional deterministic movement, caller-triggered reactivation, propagated legacy per-spawn intervals for deterministic due-reactivation at an unoccupied spawn position, and native target selection of active authoritative static IDs. Bounded TFS monster-root `experience` metadata is retained as an unsigned value; malformed or overflowing values are rejected, absent values and NPCs retain zero, and a real native selected static defeat alone routes a validated nonzero value through the configured experience award policy. When that award gains levels, configured vocation gains persist atomically with level, experience, and vitals; no gained level or matching vocation metadata leaves vital values unchanged. A core-only opt-in selector records the nearest living registered player on the same floor within a capped range, resolves equal distances by player ID, and clears its target on player removal or creature deactivation. The ordinary shared heartbeat leaves this state unchanged; an explicit nearest-living-player policy can perform one deterministic capped-range acquisition pass and reports only target-state changes. A separate explicit pursuit policy performs that acquisition and makes at most one cardinal move per active static creature. It attempts existing direct distance-reducing directions first, then uses a fixed-order breadth-first detour of at most the configured target range only when direct movement is blocked; the chosen move still reuses map/player/static-occupancy checks and advances visibility once only after a real move. Through the shared native world, a real step emits the established full-map refresh for a selected viewer; no-target, adjacent, and blocked outcomes emit nothing. A bounded static attack is available through the shared-world lock and may apply fixed damage only to an already selected, living, adjacent target; it advances the vitals-refresh epoch only after real damage. A potential lethal hit first validates the assigned temple, then enters the existing death state and clears the static target. `staticCreatureTargetAttackDamage = 0` preserves disabled behavior. A configured value from `1` through `100` makes the native heartbeat select only an immediately adjacent living player and attempt at most one fixed hit per active static creature per heartbeat. When imported direct-melee metadata exists, that creature's next attempt is blocked until its interval rounded upward to the shared one-second world clock is due; a skipped attempt does not count as applied damage. After a real attack pass, FE persists final vitals for each deterministically affected player. When the configured `deathLosePercent` is an explicit `1` through `100`, a lethal attack also commits the bounded fixed-percent post-death level, experience, vitals, skills, exact attempts, and marked lifecycle snapshot; no default-formula, blessing, promotion, or client loss packet is emitted. A connected native session observes only its own living-to-dead shared-world transition and then emits the evidence-backed zero-payload `0x28` death record once; it sends no native combat effects, respawn, teleport, timer, or loss record. An external health value of zero remains display-only and does not deactivate the entity; however, a real adjacent selected static-melee hit that reaches zero deactivates the entity, clears its target intent, removes it from active visibility, and lets the existing reactivation policy later restore its configured spawn percentage. One native game-host heartbeat advances shared-world tick, evaluates due reactivation, and can perform this explicitly configured fixed attack; client sessions no longer advance shared time. The visibility epoch changes only after real damage, deactivation, health update, or reactivation. Sub-second precision, default scheduling, unbounded/multi-step routing, movement speed, autonomous combat, creature formulas/mitigation, client effects, loot, corpses, scripts, rewards beyond this bounded source, persistent monster state, broader-profile coverage, end-to-end service lifecycle testing, and real-client observation remain deferred | deferred | deferred |
| Monster AI, respawn, combat, loot, corpses | deferred | deferred | deferred |
| NPC conversation, shops, travel | deferred | deferred | deferred |
| Lua execution and event dispatch | partial — XML/script references are validated, and audit categories enter a typed aggregate-only dispatcher that returns a deferred no-op. Separately, a direct sandbox executes one expression in a fresh vendored Lua VM with no standard libraries, capped source/memory/instruction budgets, and typed primitive-only results. An explicit callback dispatcher can register one UTF-8 callback-function file only through a caller-selected canonical root; it rejects traversal, canonical escapes, non-files, duplicate names, and existing source-limit violations, then dispatches in a fresh sandbox. The additive `script dispatch` operator command may resolve only one exact, safe relative path already declared by a chosen TFS XML registry with a `script` attribute, then pass it through that same loader with a nonempty maximum-64-byte primitive event label, a subject ID that fits Lua’s signed integer range, and one numeric value. Invalid callback input is rejected before VM construction; only primitive results cross back. It does not auto-discover scripts, execute ordinary registry content, expose filesystem/network/process/package/debug/global-host/TFS APIs, resolve modules, discover events, or mutate gameplay state; full TFS Lua compatibility remains deferred | deferred | deferred |
| Actions, movements, use/use-with, doors, levers | deferred — input is safely tolerated where implemented, not semantically executed. Native 740 map look (`0x8C`) and creature look (`0x8D`) requests are parsed into typed bounded input and acknowledged with no output; FE intentionally emits no client-visible description because unmodified OTCv8 740 lacks a usable text-message mode map. A native Quest Log request returns an explicit empty response. Item actions, doors, levers, and visible look/inspect remain deferred | deferred | deferred |
| Chat and social systems | partial — bounded shared public-chat ingestion, sanitization, recipient registration, and queue behavior, plus one native 740 public `Say` route. FE emits the parser-backed `0xAA` Talk record with speaker name, classic mode `1`, authoritative speaker position, and bounded text; a socket regression asserts the complete record. Whisper, yell, private messages, channels, statements, levels, range filtering, mute/spam rules, history, persistence, guild/party chat, scripts, and real-client confirmation remain deferred | deferred | deferred |
| Guilds, parties, trade, mail, VIP | deferred | deferred | deferred |
| Houses, depots, inboxes, rent, beds, bank/economy | partial — SQLite schema v20 adds a nonnegative durable player bank balance, initialized at zero for existing and new characters. Bounded storage APIs read or replace one exact balance, credit it without overflow, and debit it only when sufficient funds exist; all values are limited to SQLite’s signed integer range. The additive `player bank <directory> <player-id> <get|set|credit|debit> [amount]` operator command uses those APIs without changing any established command. Schema v21 persists ordered complete top-level items for the audited TFS-shaped depot IDs `0`–`19`, with at most 20 depots and 1,000 items per depot; item action and unique IDs round-trip. Schema v22 persists ordered complete top-level items for the audited TFS player inbox with its 30-item bound; item action and unique IDs round-trip. Schema v23 persists one TFS-shaped nonzero house identity to one validated owner player, with explicit assignment, reassignment, clearing, and stale-owner rejection. Map binding, rent, access lists, door permissions, beds, transfers, auctions, tile items, nested item trees, attribute blobs, depot/inbox windows, move routing, capacity rules, store inbox and market behavior, money-item conversion, NPC/client bank packets, market/trade settlement, Lua, and real-client confirmation remain deferred | deferred | deferred |
| PvP skulls/frags/bans/death policy | deferred | deferred | deferred |
| Backups | supported — local SQLite copy plus manifest | supported | supported |
| TFS conversion audit | partial — safe registry/entity/spawn-reference inventory and missing/unsafe-path diagnostics | partial | partial |

## Stable local-world CLI contract

The following commands are part of FE’s public local-first interface. Their basic syntax and concise default output are preserved. Future features must be additive through flags or subcommands; no command may be removed or renamed without an alias and two release cycles of deprecation.

| Command | Current purpose | Compatibility requirement |
|---|---|---|
| `init <directory> [--profile …]` | Creates a world directory, template config/content skeleton, and SQLite database. | Preserve the world-directory workflow and default profile behavior. |
| `validate <directory>` | Loads configuration, reconciles the content skeleton, validates map/content, and opens the database. | Remain non-destructive and concise. |
| `tfs-audit <directory>` | Audits selected private TFS-style content without executing Lua. | Continue to report conversion readiness and explicit runtime deferrals. |
| `run <directory> [--ed]` | Starts services; `--ed` enables bounded privacy-safe diagnostics. | Keep normal output concise; never log credentials or raw packet bodies. |
| `status <directory>` | Reports local server/profile/database status. | Preserve the no-panel local inspection path. |
| `generate-key <directory>` | Generates an FE-owned legacy key where applicable. | Keep overwrite protection and profile validation. |
| `backup <directory>` | Creates a local SQLite backup and manifest. | Preserve simple one-command recovery preparation. |
| `account create <directory> <account-name> <password>` | Creates a local account without database-console access. | Preserve Argon2-backed local provisioning and numeric account compatibility. |
| `player create <directory> <account-id> <character-name>` | Creates a local test character. | Preserve the current numeric account/character workflow. |
| `player vocation <directory> <player-id> <vocation-id>` | Stores a numeric vocation identity for an existing local character. | Additive only; custom IDs remain valid pending operator-loaded vocation-rule runtime. |
| `player skill <directory> <player-id> <skill> <level> [percent]` | Stores a bounded typed classic skill value for an existing local character. | Additive only; no skill-try or advancement-formula claim. |
| `command <directory> broadcast <message>` | Records the bounded administrator broadcast command. | Extend only through an allowlisted typed command family. |
| `compatibility`, `version`, `help` | Gives profile/capability/build guidance. | Remain stable, discoverable, and scriptable. |

## Change-control rules

1. A feature is not listed as `supported` unless its core behavior, persistence behavior where relevant, and profile-specific protocol delivery are all tested.
2. A content parser does not imply runtime execution. Script references, weapons, spells, and actions stay `deferred` until their runtime semantics exist.
3. The capability key is **profile + content adapter + declared feature**, not the FE release number alone.
4. Private maps, item data, client assets, and TFS data packs are never committed to this repository or used as FE fixtures.
5. Every new command or command option requires help/exit-code regression coverage and must preserve normal versus `--ed` privacy/output guarantees.
