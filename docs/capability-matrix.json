{
  "schemaVersion": 1,
  "statusVocabulary": [
    "supported",
    "partial",
    "deferred",
    "not-applicable",
    "unknown"
  ],
  "profiles": [
    {
      "id": "fe-7.4",
      "target": "Tibia 7.4",
      "nativeOtClientV8": "partial",
      "nativeOtClientV8Detail": "experimental 740 login, character selection, map, movement, shared-player visibility, bounded player-vitals updates, parser-backed LookMap and visible LookCreature status-text responses, and a parser-shaped zero-count Quest Log acknowledgement. Active peers render the shared authoritative classic outfit, direction, and bounded health percentage. An actual shared-vital change triggers the existing complete viewport refresh after each session’s parser-shaped self stats update. Accepted turns and moves advance the shared visibility epoch and re-render visible peer sessions through the same full-map path. A native socket regression proves the Quest Log response leaves the session usable for a following outfit-window request. LookMap requires an exact catalog-mapped authoritative map item. LookCreature permits only a known player or active static creature on the same floor inside the current classic 18×14 viewport, and otherwise sends no packet. The player-stats record has parser-shaped 16-bit level and soul fields, but unmodified-client confirmation remains required",
      "nativeFixedEquipmentLook": "partial",
      "nativeFixedEquipmentLookDetail": "ordinary native LookMap additionally accepts only x = 0xFFFF, one unflagged fixed equipment-slot code, z = 0, zero stack position, and an exact validated catalog client item ID that matches the caller's current authoritative slot item. It returns the existing parser-shaped status text with slot code, server item ID, count, and optional bounded imported legacy name and weight details. Container positions, nonzero stacks, map items beyond the existing route, TFS articles/pluralization, descriptions, attributes, and generic inspection remain deferred",
      "nativeTopLevelContainerLook": "partial",
      "nativeTopLevelContainerLookDetail": "ordinary native LookMap additionally accepts one current open caller-owned non-nested top-level container item only through its classic flagged x = 0xFFFF address, current window ID, exact item index, zero stack position, and exact catalog client item match. It returns the existing parser-shaped status text with container ID, server item ID, count, and optional bounded imported legacy name plus stack-aware weight details. Closed windows, nested containers, absent indexes, mutation, TFS articles/pluralization, descriptions, attributes, and generic inspection remain deferred",
      "officialClientProtocolEmulation": "deferred",
      "localCharacterProvisioning": "supported",
      "localCharacterProvisioningDetail": "local SQLite/Argon2 account provisioning and additive character creation persist the exact classic cumulative experience threshold for the returned level-eight default, so later authoritative experience awards cannot reduce a newly created character's level. The stable player create command preserves its original account/name form and accepts an optional validated u16 vocation ID that is durable at insertion; no label-to-vocation inference is claimed. Existing externally created or legacy inconsistent character records are not rewritten automatically",
      "contentAudit": "partial",
      "legacyItemMetadata": "partial",
      "nativeEquipmentArmorMitigation": "partial",
      "nativeEquipmentArmorMitigationDetail": "FE derives the target's profile-neutral flat physical reduction from positive validated items.xml armor values in authoritative head, neck, armor, legs, feet, and ring slots. The capped sum is multiplied deterministically in thousandths by the validated parsed vocation armor multiplier, or the safe 1.000 default when none is available. It is recalculated from current authoritative equipment before each typed declarative selected-player weapon event. The same derived state applies before the existing opt-in fixed static-creature physical attack evaluates damage and death. It is not stored separately; session hydration applies the same derived state. Hands, shields, item defense/extra-defense, the vocation defense multiplier, fight mode, active block, random armor, resistance, fixed fallback player melee, packets, and TFS formula parity remain deferred",
      "legacyItemMetadataDetail": "bounded operator-supplied items.xml armor, weight, slot-type labels, defense, extra-defense, attack-speed, and verified weapon-type labels are retained only for known OTB items. Optional legacy unsigned weight values and finite slot-type sets are retained by authoritative server ID as import-only metadata. Positive armor is the sole parsed field with an active bounded use through nativeEquipmentArmorMitigation; weight, slot-type, defense, extra-defense, attack-speed, and weapon-type metadata do not alter FE combat, capacity, equipment placement, packets, or compatibility claims. Full formula, capacity, and placement use remain deferred until profile-specific inventory, container, vocation, and equipment contracts are implemented",
      "runtimeItemsAndInventory": "partial",
      "runtimeItemsAndInventoryDetail": "bounded item/equipment/container core, SQLite equipment/container persistence, native-session hydration, catalog-validated classic-740 initial equipment delivery, and stable operator commands that atomically move a complete item from equipment into one owned bounded top-level container or from an owned container item index into an empty equipment slot. Occupied slots, missing containers, and invalid indices leave authoritative state unchanged. A shared authoritative equipment replacement advances a dedicated epoch; a native session compares only catalog-mapped slots already sent at login and emits deterministic parser-verified SetInventory or DeleteInventory records for a changed, removed, or newly unmapped slot. A real shared container replacement advances a dedicated epoch and re-emits only current complete catalog-mapped top-level open-container records. Native 740 CloseContainer (0x87) is decoded as one client-view container ID and receives the parser-verified 0x6F close record; that session ID is excluded from later container refreshes without mutating shared or persisted data. Native 740 UpArrowContainer (0x88) is decoded as one client-view container ID, but no parent, nested, or browse-field container model exists yet, so it sends no packet and leaves session, shared, and persisted state unchanged. Native 740 UpdateContainer (0xCA) requests one complete mapped top-level container for the current session, sends its existing parser-verified 0x6E record when available, and removes that ID from the closed-view set without mutating shared or persisted data. Native 740 ThrowItem (0x78) may move one catalog-mapped whole item from a fixed equipment slot into an already-owned mapped top-level container window after validating the classic flagged address, source identity, whole-item count, ownership, and non-nested destination. The authoritative move persists both equipment and containers and then uses the existing DeleteInventory plus complete container-refresh frames. The destination index remains a client drop location for that whole-item append. A requested partial or full source count may instead move from a mapped equipment stack into an existing matching item at the exact owned non-nested top-level container index; the bounded authoritative merge persists both collections and uses existing SetInventory or DeleteInventory plus complete container-refresh frames. The reverse native ThrowItem move may transfer one catalog-mapped whole item from an owned non-nested top-level container index into an empty fixed equipment slot after validating source identity and count. A requested partial count may instead move from that owned non-nested top-level container item into an empty or existing matching fixed equipment item; the bounded authoritative merge persists both collections and uses existing SetInventory plus complete container-refresh frames. Whole-item occupied-slot swaps, nonmatching stack destinations, nested containers, map-ground transfers, capacity policy beyond the bounded container rejection, generic item controls, and real-client confirmation remain deferred",
      "nativeFullContainerEquipmentStackMerge": "partial",
      "nativeFullContainerEquipmentStackMergeDetail": "a mapped requested source count equal to the entire owned non-nested top-level container item may merge into a fixed equipment destination only when that destination already holds the same mapped item ID. FE persists the resulting empty container and merged equipment stack, then emits the existing SetInventory and complete-container refresh records. A live authenticated socket regression verifies a 30-count full-source merge into a 30-count equipment stack. Nonmatching occupied slots, swaps, nested containers, map-ground transfers, capacity policy, generic inventory controls, and real-client confirmation remain deferred",
      "nativeCloseContainerView": "partial",
      "nativeCloseContainerViewDetail": "native 740 CloseContainer decodes one client-view container ID, returns the parser-verified 0x6F close record, and filters that ID from later top-level container refreshes for the current session only. It does not mutate shared or persisted container state; nesting, up-arrow, and item-delta controls remain deferred",
      "nativeUpArrowContainerView": "partial",
      "nativeUpArrowContainerViewDetail": "native 740 UpArrowContainer decodes exactly one client-view container ID. FE currently has no verified parent, nested, or browse-field container model, so the accepted request produces no packet and changes no session, shared, or persisted container state. Parent traversal, nesting, browse fields, item deltas, and real-client confirmation remain deferred",
      "nativeUpdateContainerView": "partial",
      "nativeUpdateContainerViewDetail": "native 740 UpdateContainer decodes one client-view container ID and reopens only that current complete catalog-mapped top-level container through the parser-verified 0x6E record. A successful reopen removes the ID from the current session’s closed-view set; unavailable, nested, incomplete, or unmapped containers produce no packet and no state mutation. Shared/persisted container state, nesting, up-arrow, and item-delta controls remain deferred",
      "playerState": "partial",
      "playerStateDetail": "persisted health, mana, capacity, and magic level feed a parser-shaped classic 740 HUD record with a 16-bit level and bounded soul byte. A shared-world vocation level-up advances the stats epoch, then the existing native refresh rehydrates authoritative level, experience, and vitals before encoding the frame. A validated native static-creature defeat is now one bounded gameplay experience source: when it crosses a level threshold and the player's vocation has configured gains, the level, experience, current/max health, current/max mana, and capacity snapshot persist atomically. Unmodified-client confirmation remains required",
      "playerPosition": "partial",
      "playerPositionDetail": "authoritative native movement persists SQLite position and orderly plus abrupt-disconnect native-session relog regressions restore the saved tile. Unmodified-client confirmation remains required",
      "fightModeState": "partial",
      "fightModeStateDetail": "parser-backed native 740 fight-mode, chase-mode, and secure-mode state uses verified attack/chase-off/secure-off defaults and accepted input updates synchronize through authoritative shared state. One parser-verified three-byte 0xA7 player-modes record is appended to native 740 initialization and is sent again only after a real accepted state change; identical repeated input does not create a redundant record. Later PvP-mode variants, mitigation, attack behavior, pursuit, persistence, and real-client confirmation remain deferred",
      "playerOutfit": "partial",
      "playerOutfitDetail": "schema-v11 persists accepted classic-740 look type and color values, then hydrates them after map initialization with a configured look-type fallback for legacy or out-of-range records. A native 0xD2 request receives a classic 0xC8 current-outfit plus one-byte inclusive start/end look-type range. Operators may set otclientV8OutfitFirstLookType and otclientV8OutfitLastLookType; both must fit the classic byte form and contain the configured current look type, while zero values preserve the current-look default for existing worlds. A concrete ChangeOutfit request inside that range persists and echoes the selected appearance; an out-of-range request retains the current stored appearance. On an accepted change, the shared native world records the complete validated classic outfit, advances its visibility epoch, and visible peer sessions re-render that authoritative look type and four colour bytes through their existing full-map refresh. Newer outfit-list, addon, and mount fields are not emitted. Asset validation and unmodified-client confirmation remain deferred",
      "lifecycle": "partial",
      "vocationCombatFormulaMetadata": "partial",
      "vocationCombatFormulaMetadataDetail": "bounded nested TFS-style formula defense and armor values are parsed as validated fixed-point metadata, with independent 1.000 defaults when absent. The parsed armor multiplier now scales only FE's existing capped six-slot armor-only reduction through deterministic integer thousandths. The defense multiplier, fight modes, active blocking, controlled randomness, remaining equipment aggregation, client packets, and wider profile-specific formulas remain deferred",
      "vocationTimingMetadata": "partial",
      "vocationTimingMetadataDetail": "bounded TFS-style vocation attackspeed and basespeed attributes are retained as typed metadata, with verified defaults of 1500 milliseconds and 220 when absent. They do not alter current FE movement, combat cooldowns, packets, or profile compatibility claims; profile-specific timing, item-versus-vocation precedence, and movement formula behavior remain deferred",
      "lifecycleDetail": "server-side death, temple respawn, and schema-v9 persisted dead/respawn/loss state with strict native hydration. A configured explicit deathLosePercent value from 1 through 100 now applies the bounded exact fixed-percent loss model after supported native player-melee, condition, or static-creature heartbeat deaths, then commits level, experience, vitals, visible skills, exact progression attempts, and marked lifecycle state in one SQLite transaction. Zero preserves no-loss behavior. The native 740 path emits the evidence-backed zero-payload 0x28 death notification and sends no client loss record. Public OTCv8 evidence shows its death dialog safely logs out and begins a new character login rather than sending a revival request. When that next FE native game session authenticates a persisted-dead character, FE invokes the existing authoritative temple-respawn transition before shared-world registration, validates the target tile, atomically persists position/vitals/cleared lifecycle state, and delivers the temple through the normal parser-shaped full-map bootstrap. Death-screen timing, automatic timers, direct revive/teleport/loss records, default-loss formula (-1), blessing/promotion adjustments, and unmodified-client confirmation remain deferred",
      "progression": "partial",
      "progressionDetail": "typed seven-skill levels/percentages and numeric vocation identity are authoritative, SQLite-persisted, locally provisionable, and delivered to the selected native 740 path. Operator-owned TFS-style vocations.xml supplies online health/mana regeneration intervals, skill multipliers, and parsed gainhp, gainmana, and gaincap metadata. A bounded direct config.lua experienceStages literal table accepts documented minlevel, optional maxlevel, and multiplier fields without Lua execution, rejects expressions, duplicate/unknown fields, overlap, invalid values, and excess data, and replaces rateExp when present; experienceStages = nil selects flat rateExp. The existing data/XML/stages.xml adapter is a fallback only when the legacy assignment is absent and retains its established rateExp composition. All selected sources drive deterministic authoritative operator experience awards with SQLite persistence, and existing experience awards and loss resolve levels through a checked integer classic cumulative threshold. When a matching parsed vocation has a positive level increase, the stable CLI and bounded native static-defeat source atomically persist scaled gains to current/max health, current/max mana, and capacity alongside the new level and experience; absent metadata or no level increase leaves vitals unchanged. The matching shared-world host transition advances the profile-gated stats-refresh epoch and rehydrates the next 740 frame from authoritative level, experience, and vitals. Each successful fixed selected-player melee hit can award one configured fist try with exact persisted counters. Hydrated bounded poison/burning/energy damage schedules advance on the native heartbeat with persisted vitals; schema-v10 preserves each validated elapsed interval remainder across restart and a fresh native-style relog registration, while client condition-effect packets remain deferred. Lethal bounded condition damage activates the existing persisted server-side death state only at a validated assigned town temple. For configured explicit fixed loss only, the same progression rules now back an atomic post-death loss snapshot; default loss, promotion, and blessing behavior remain unimplemented. Weapon/spell formulas, combat/quest rewards, client death screens/timers/respawn packets, soul, and full TFS-compatible advancement transactions remain deferred",
      "combat": "partial",
      "nativeFixedSelectedPlayerMelee": "partial",
      "nativeFixedSelectedPlayerMeleeDetail": "the existing fallback now constructs a server-owned typed adjacent physical combat event with a one-server-tick interval. This preserves the shared elapsed-second heartbeat cadence, prevents duplicate same-tick damage, applies the current bounded armor-only reduction, and reuses existing persisted damage/death delivery. TFS weapon timing, attack speed, skills, shields, fight modes, random blocking, PvP legality, resistance, effects, automatic attacks, and formula parity remain deferred",
      "combatDetail": "typed physical adjacent-melee events use bounded damage, deterministic per-player server-tick cooldowns, profile-neutral flat physical mitigation, and an optional scriptless FE declarative weapon catalog. On the current native selected-melee path, only a server-owned matching right-hand item may select a catalog weapon; otherwise no catalog event is created. Separately, an active adjacent selected static target receives fixed 10-percentage-point damage no more than once per authoritative world tick through the same per-player combat cooldown. A supported native lethal player-combat transition may now apply and atomically persist the explicit configured fixed death-loss model after validated town/temple state; it sends no additional client loss packet. The parser-backed zero-payload native 740 cancel-attack/follow control (0xBE) clears only the current authenticated player's existing authoritative target and follow intents; it sends no record and adds no combat behavior. A rejected nonzero native target selection receives the parser-verified zero-payload classic ClearTarget record (0xA3); accepted targets and manual clear send no additional feedback. Native selection and follow reject an authoritative player marked dead before changing the source interaction intent. When bounded selected-static melee deactivates that same selected target, the initiating session receives one ClearTarget record before the existing zero-health and visibility delivery; nonlethal hits send none. When the opt-in native static-target policy applies real damage, it persists the resulting vitals for the deterministic affected-player set and persists the existing death state for a lethal valid-temple transition; weapon catalog, mitigation, weapon timing, rewards, client effects, loot, corpses, formulas, scripts, and full AI remain separate. A typed spell-cast foundation atomically enforces nonzero bounded mana costs and server-tick cooldowns; an optional declarative spell catalog may construct those events from ID, mana cost, and interval, is retained as immutable native-host configuration, and can be resolved through a synchronized server-only host helper. No spell layer is profile-gated, persistent, or client-routable. TFS Lua weapon scripts, legacy XML formula semantics, map use, client weapon-use packets, armor, shielding, spell names/words/targets/formulas/effects/packets, ranged delivery, resistance, PvP, automatic attacks, cross-player target cleanup, and client combat effects remain deferred",
      "inventory": "partial",
      "mapItemUse": "partial",
      "nativeReadOnlyTextWindow": "partial",
      "nativeReadOnlyTextWindowDetail": "after exact native 740 UseItem validation of one catalog-mapped top-level map stack, FE opens a parser-verified read-only 0x96 text window only when that same imported map item has non-empty OTBM text metadata. The frame carries window ID zero, the validated client item ID, text byte length, text, and empty writer only; later writable-date and traded fields are omitted. The route makes no map, shared, or persistence mutation. Editing, text-save input, writer/date metadata, containers, scripts, descriptions, and generic item actions remain deferred",
      "nativeSteppedOnTeleport": "partial",
      "nativeSteppedOnTeleportDetail": "after one validated native cardinal or diagonal move lands on a tile with exactly one top-level imported teleport destination, FE revalidates its destination under the authoritative lock. It resolves at most eight direct imported teleport hops, stops safely on a missing, ambiguous, blocked, repeated, or hop-limited target, then persists only the final successful position and sends one full viewport refresh instead of normal movement frames. Effects, scripts, generic tile actions, and real-client confirmation remain deferred",
      "mapItemUseDetail": "typed server-side validation accepts only same-tile or adjacent use of an exact top-level authoritative map item stack and returns immutable metadata; the same validation is available through synchronized host state. Native 740 0x82 input is decoded as position, client thing ID, stack position, and index, then reaches that validator only when the configured presentation catalog maps the client thing ID to exactly one server item ID. For one exact validated item with imported teleport-destination metadata, FE performs the sole supported native item action: it rechecks that destination as walkable and unoccupied under the authoritative world lock, performs the distinct explicit player relocation, persists the new position, cancels active click-walk, advances native visibility, and sends the existing full viewport refresh. A blocked destination produces no mutation or packet. Native 740 UseItemEx (0x83) is decoded as two position/client-thing/stack references and reaches a matching two-target validator only when both IDs map uniquely; both server-owned top-level items independently pass the existing map and range checks. Native 740 battle-window input (0x84) is decoded as one source position/client-thing/stack reference plus one creature ID; its source requires a unique mapping and its target must be a live player or active static creature on the same or an adjacent tile. Native 740 rotate-item input (0x85) is decoded as one position/client-thing/stack reference and reaches existing exact map-item validation only when its client item ID maps uniquely. Missing or ambiguous mappings, absent maps, and invalid source or target items are rejected without an outbound packet or mutation. All other action execution, rotation, doors, switches, containers, charges, Lua, target selection, combat, messages/effects, and full client item-use behavior remain deferred",
      "nativeMapInspection": "partial",
      "nativeMapInspectionDetail": "Native 740 LookMap (0x8c) validates one uniquely catalog-mapped exact authoritative map item and returns a parser-verified 0xb4 status-text record with classic message class 0x15. The base response exposes server item ID and count; when that exact source item has imported legacy name and weight metadata, FE appends bounded Name: and stack-aware hundredths-of-an-ounce details before an existing size-bounded imported map description. Absent maps, mappings, or items send no packet. Native 740 LookCreature returns that same status record only for a known active static creature or known player on the same floor inside the exact current classic 18x14 viewport, exposing only the validated name. Missing, inactive, cross-floor, or off-screen targets send no packet. TFS articles, pluralization, attributes, recursive container weight, capacity, health/level, and every other inspection format remain deferred",
      "nativeLookMapImportedDescription": "partial",
      "nativeLookMapImportedDescriptionDetail": "after existing exact native 740 LookMap validation, FE may append one bounded legacy items.xml name, one imported legacy item weight in hundredths-of-an-ounce form, and one non-empty imported OTBM description from that same top-level stack to the parser-verified 0xb4/0x15 status record. Weight uses source OTB stackability only for the displayed total. Missing or mismatched metadata, absent name or weight, empty description, or a frame-oversized addition retains the preceding bounded text. TFS article/plural forms, attributes, recursive container weight, capacity, text content, writable metadata, and broader inspection behavior remain deferred",
      "equipmentBootstrap": "partial",
      "equipmentBootstrapDetail": "catalog-mapped persisted equipment is emitted at native 740 login in deterministic multi-slot order. Extended diagnostics report the count of omitted unmapped items without packet bodies. Bounded native 740 whole-item ThrowItem routes can move a mapped fixed-slot item into an existing owned top-level mapped container or move a mapped owned top-level container item into an empty fixed slot; swaps, partial stacks, and generic inventory remain deferred",
      "containerBootstrap": "partial",
      "nativeContainerToContainerTransfer": "partial",
      "nativeContainerToContainerTransferDetail": "a mapped item in one current caller-owned non-nested top-level container may move a requested bounded count into a distinct current caller-owned non-nested top-level container after exact source ID, count, and ownership validation. FE merges only an identical item instance or appends one bounded stack, atomically persists both windows, and advances the existing container refresh epoch. Same-container ordering, nested or closed windows, swaps, ground/depot/inbox/trade, capacity/weight, scripts, and generic inventory behavior remain deferred",
      "containerBootstrapDetail": "top-level persisted containers are emitted at native 740 login only when the container and every contained item have validated presentation mappings. Nested or incomplete-mapping windows are omitted. Bounded native 740 whole-item ThrowItem routes may append one catalog-mapped fixed-slot item to an already-owned mapped top-level container or remove one catalog-mapped item into an empty fixed equipment slot, refreshing the complete open-container record; nested windows, swaps, stacks, generic controls, and real-client confirmation remain deferred",
      "questLog": "partial",
      "questLogDetail": "the native 740 request is decoded and receives a parser-shaped zero-count quest response. A native socket regression verifies the exact response and a following parser-backed outfit-window request in the same session; quest storage, mission lines, scripting, gameplay semantics, and unmodified-client confirmation remain deferred",
      "nativeSelectedStaticMelee": "partial",
      "nativeSelectedStaticMeleeDetail": "an active adjacent static creature selected by the native player receives bounded fixed 10-percentage-point damage no more than once per authoritative world tick. Each real hit writes the parser-shaped native 740 CreatureHealth record immediately and persists the authoritative static runtime snapshot; a defeated creature is removed through the existing visibility refresh. On a real defeat only, validated nonzero raw monster experience passes through the configured existing rate-and-stage policy. A positive gained-level result applies the selected player's configured vocation health, mana, and capacity gains, and atomically persists the resulting level, experience, and vitals; missing vocation metadata or no gained level keeps existing vitals. The existing native progression HUD-refresh epoch then delivers the authoritative state. Loot, corpses, formulas, other rewards, scripts, autonomous AI, broader-profile coverage, and real-client confirmation remain deferred",
      "legacyMonsterExperienceMetadata": "partial",
      "legacyMonsterExperienceMetadataDetail": "the bounded non-executing TFS monster definition parser retains an unsigned root experience attribute for monster records. Malformed or overflowing values are rejected, while missing values and all NPC records retain zero. A nonzero materialized monster value is awarded only after the existing native selected-static melee transition actually defeats that creature, through the configured rate-and-stage policy. A positive level increase applies the player's configured vocation vital gains and atomically persists level, experience, and vitals. Loot, corpses, formulas, scripts, AI, and client effects remain separate",
      "nativeStaticCreaturePursuit": "partial",
      "nativeStaticCreaturePursuitDetail": "staticCreatureTargetPursuitRange = 0 preserves disabled behavior. A configured value from 1 through 8 enables at most one deterministic nearest-living-player cardinal pursuit move for each active static creature per native heartbeat. FE attempts existing direct distance-reducing directions first, then applies a fixed-order breadth-first detour limited to the configured range only when direct movement is blocked; the chosen move still uses existing map and occupancy checks and advances visibility only after a real move. Unbounded or multi-step routing, movement speed, retry scheduling, combat, loot, scripts, NPC behavior, and real-client confirmation remain deferred",
      "nativePlayerFollow": "partial",
      "nativePlayerFollowDetail": "a living current player follow target permits at most one direct cardinal distance-reducing source step per native heartbeat through authoritative walkability and occupancy checks; a successful batch advances visibility once and persists source positions. Pathfinding, diagonals, retries, facing, attack automation, formations, and full TFS follow parity remain deferred",
      "creaturesDetail": "catalog-derived static spawn representation, native visibility, bounded authoritative 0–100% display health delivered on login and visibility refresh, optional deterministic movement, caller-triggered reactivation, propagated legacy per-spawn intervals for deterministic due-reactivation at an unoccupied spawn position, and native target selection of active authoritative static IDs. A core-only opt-in selector records the nearest living registered player on the same floor within a capped range, resolves equal distances by player ID, and clears its target on player removal or creature deactivation. The ordinary shared heartbeat leaves this state unchanged; an explicit nearest-living-player policy can perform one deterministic capped-range acquisition pass and reports only target-state changes. A separate explicit pursuit policy performs that acquisition and attempts at most one distance-reducing cardinal step per active static creature, reusing map/occupancy checks and advancing the visibility epoch once only if any real move occurs. Through the shared native world, a real step emits the established full-map refresh for a selected viewer; no-target, adjacent, and blocked outcomes emit nothing. A bounded static attack is available through the shared-world lock and may apply fixed damage only to an already selected, living, adjacent target; it advances the vitals-refresh epoch only after real damage. A potential lethal hit first validates the assigned temple, then enters the existing death state and clears the static target. staticCreatureTargetAttackDamage = 0 preserves disabled behavior. A configured value from 1 through 100 makes the native heartbeat select only an immediately adjacent living player and apply at most one fixed hit per active static creature per heartbeat. After a real attack pass, FE persists final vitals for each deterministically affected player. When the configured deathLosePercent is an explicit 1 through 100, a lethal attack also commits the bounded fixed-percent post-death level, experience, vitals, skills, exact attempts, and marked lifecycle snapshot; no default-formula, blessing, promotion, or client loss packet is emitted. A connected native session observes only its own living-to-dead shared-world transition and then emits the evidence-backed zero-payload 0x28 death record once; it sends no native combat effects, respawn, teleport, timer, or loss record. An external health value of zero remains display-only and does not deactivate the entity; however, a real adjacent selected static-melee hit that reaches zero deactivates the entity, clears its target intent, removes it from active visibility, and lets the existing reactivation policy later restore its configured spawn percentage. One native game-host heartbeat advances shared-world tick, evaluates due reactivation, and can perform this explicitly configured fixed attack; client sessions no longer advance shared time. The visibility epoch changes only after real damage, deactivation, health update, or reactivation. Default scheduling, multi-step routing, autonomous combat, obstacle pathfinding, creature formulas/mitigation, client effects, loot, corpses, scripts, rewards, persistent monster state, broader-profile coverage, end-to-end service lifecycle testing, and real-client observation remain deferred",
      "scripting": "partial — imported script references still enter the aggregate no-op audit dispatcher by default. A separate direct expression sandbox creates a fresh vendored Lua VM with no standard libraries, capped source/memory/instruction budgets, and typed primitive-only results. An explicit callback dispatcher can register one UTF-8 callback-function file only through a caller-selected canonical root; it rejects traversal, canonical escapes, non-files, duplicate names, and existing source-limit violations, then dispatches in a fresh sandbox. The additive script dispatch command may resolve only one exact, safe relative path already declared by a selected TFS XML registry with a script attribute, then pass it through that same loader with bounded primitive input. It does not auto-discover scripts, execute ordinary registry content, expose filesystem, network, process, package, debug, host, TFS API, module loading, event discovery, or gameplay mutation; full TFS Lua compatibility remains deferred",
      "socialAndEconomy": "deferred"
    },
    {
      "id": "fe-8.0",
      "target": "Tibia 8.0",
      "nativeOtClientV8": "partial",
      "nativeOtClientV8Detail": "explicit profile foundation recognizes that classic 8.0 requires RSA/XTEA transport, includes a bounded outbound-only opaque XTEA envelope primitive, and can extract only the marker plus four XTEA words from an already decrypted fixed block for future parser-backed work. Native enablement remains refused: there is no client-input decryption, credential/login/game parser, listener, packet-layout claim, or client interoperability claim",
      "officialClientProtocolEmulation": "deferred",
      "contentAudit": "partial",
      "runtimeItemsAndInventory": "deferred",
      "playerState": "deferred",
      "progression": "deferred",
      "combat": "deferred",
      "creatures": "deferred",
      "scripting": "deferred",
      "socialAndEconomy": "deferred"
    },
    {
      "id": "fe-1.2",
      "target": "TFS 1.2 / Tibia 10.98",
      "nativeOtClientV8": "deferred",
      "officialClientProtocolEmulation": "deferred",
      "contentAudit": "partial",
      "runtimeItemsAndInventory": "deferred",
      "playerState": "deferred",
      "progression": "deferred",
      "combat": "deferred",
      "creatures": "deferred",
      "scripting": "deferred",
      "socialAndEconomy": "deferred"
    }
  ],
  "cliCompatibility": {
    "policy": "additive-only",
    "normalRunOutput": "concise",
    "extendedDiagnosticsFlag": "--ed",
    "diagnosticsMayExposeCredentials": false,
    "diagnosticsMayExposeRawPacketBodies": false,
    "deprecatedCommandMinimumReleaseOverlap": 2
  }
}
