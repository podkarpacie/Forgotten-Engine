# Development Checklist

- [x] Define safe local `account create` and `player create` command behavior for native-world testing.
- [x] Implement Argon2-backed account creation and account-owned character creation through the CLI.
- [x] Add unit and CLI-level coverage for provisioning, duplicate handling, and native numeric account lookup.
- [x] Build and publish updated Linux and Windows binaries with the provisioning commands.
- [ ] Diagnose and correct the reported native OTCv8 login rejection after local account provisioning.
- [ ] Diagnose and correct the native OTCv8 game-session disconnect after successful character selection.
- [ ] Clarify the intentionally minimal character fixture and defer professions/vocations to the player-state milestone.
- [ ] Diagnose and correct the native OTCv8 client-side `ERROR 2` after successful character selection and retained server listener state.
- [ ] Add wire-level native game-session diagnostics and correct the remaining 740 post-selection initialization mismatch.
- [ ] Correct the native 740 game-login request decoder after the live client exposed `StringTooLong(1536)` before initialization.
- [ ] Correct the native 740 game-start transition after the client receives login/map frames but returns to character selection.
- [ ] Correct the outbound native 740 initialization stream after the real client emits no post-map control frame.
- [ ] Remove the invalid zero extended-outfit player creature from the asset-free native map diagnostic fixture.
- [ ] Correct remaining native 740 game-start packet sequencing after map-only initialization still returns to character selection.
- [ ] Treat Windows nonblocking `WouldBlock` during a native session as a transient condition rather than a rejected game session.
- [x] Send the native 740 player bootstrap packets required for non-negative HUD statistics and the local-player identity.
- [x] Restore a visible, profile-selected empty-world player/map diagnostic fixture without bundling client assets.
- [x] Keep a native 740 active session alive by replying to the real client keepalive sequence.
- [x] Decode click-to-walk (`0x14`) without dropping an otherwise valid native 740 session.
- [x] Decode and safely acknowledge classic 740 chat traffic without dropping the session.
- [x] Preserve the local player’s mapped creature identity when sending cardinal movement updates.
- [x] Stream a replacement map viewport or reject the move cleanly before a native empty-world boundary causes a client disconnect.
- [x] Pace native click-to-walk updates so OTCv8 visibly walks the path rather than appearing to teleport.
- [x] Decode classic Ctrl-turn client opcodes and preserve the requested facing direction without disconnecting.
- [x] Display a bounded diagnostic acknowledgement for accepted 740 chat input.
- [x] Preserve the current facing direction when canceling a boundary walk.
- [x] Replace fixed native auto-walk delay with speed-derived timing from the player/configured movement speed.
- [x] Add regression coverage proving faster configured speed shortens the native auto-walk interval while retaining safe bounds.
- [x] Cancel an active native auto-walk path when a manual cardinal move, turn, stop, or leave frame arrives.
- [x] Prevent queued auto-walk steps from racing later client input or causing apparent teleporting.
- [x] Emit accepted 740 chat through a parser-backed client-visible `Say` message mode. Protocol and native socket regressions verify the complete `0xAA` name/mode/position/text record; real-client rendering remains separately pending.
- [x] Accept stock 740 right-click use/look frames without terminating the native session.
- [x] Establish and implement a verified OTCv8 740 client-visible chat layout without using an invalid generic text-message mode. FE uses only the local parser-backed Talk `0xAA` layout with `Say` mode `1`; other chat modes remain deferred.
- [x] Cancel and discard an in-flight auto-walk when a newer manual client action arrives.
- [x] Serialize rapid click/manual movement so only one authoritative native movement update is in flight at a time.
- [x] Stop sending the parser-invalid 740 server text message until its exact supported layout is confirmed.
- [x] Accept and safely handle the stock 740 outfit-window/change interaction frame.
- [x] Encode a bounded stock 740 server-speech record that OTCv8 renders for local diagnostic chat.
- [ ] Verify the visible chat echo with a real unmodified OTCv8 740 client without parser errors.
- [x] Audit currently reachable stock OTCv8 740 game actions for session-ending unsupported frames.
- [x] Safely consume high-frequency selection, combat-target, follow, look, and inventory interaction frames until their full game semantics exist.
- [x] Add compatibility regression coverage for all newly tolerated native action frames.
- [x] Define and validate a bounded operator-owned FE map document without redistributing map or client content.
- [x] Load the selected map from the world data directory and use it to construct native 740 viewport tiles.
- [x] Enforce map walkability and emit safe streamed viewport updates when a player moves beyond the prior diagnostic rectangle.
- [x] Add map-loading, walkability, and streamed-view regression coverage for the native 740 host.
- [x] Select an original Rust world document through TFS-style `mapName` and `data/world/<mapName>.femap` conventions.
- [x] Define a clean-room compatibility contract for operator-supplied TFS/OpenTibia OTBM, XML companion data, and item-definition versions.
- [x] Extend the FE map model for ordered legacy tile items, map metadata, spawn areas, towns, and houses while retaining editable `.femap` support.
- [x] Implement a bounded clean-room OTBM reader and an explicit FE import/export workflow without redistributing private map data.
- [x] Load companion `spawns.xml` and `houses.xml` selected by the operator’s OTBM world configuration while preserving embedded OTBM towns.
- [x] Load operator-supplied item-definition metadata needed for walkability and client-visible tile layers.
- [x] Encode layered imported world tiles and movement semantics through the native OTCv8 740 map path.
- [x] Add hand-authored compatibility fixtures and tests for OTBM, XML companion data, item metadata, import/export, viewport encoding, and walkability.
- [x] Package and publish the legacy TFS world-data compatibility release with Linux, Windows, and SHA-256 assets.
- [x] Audit a representative TFS server directory and `config.lua` contract against FE startup and native-profile requirements.
- [x] Accept TFS-compatible configuration defaults and discover standard world, item, spawn, and house file locations without FE-only rewrites.
- [x] Add an explicit CLI conversion-readiness audit that distinguishes importable content from unavailable FE runtime systems.
- [x] Add self-contained representative TFS-layout fixtures covering config, OTBM, items, and companion-file discovery.
- [x] Package and publish the TFS conversion-readiness release with Linux, Windows, and SHA-256 assets.
- [x] Audit standard TFS XML registry formats and identify the safe FE runtime boundary for referenced script content.
- [x] Load bounded TFS content registries and validate every referenced relative file path without executing scripts.
- [x] Produce a typed conversion inventory that distinguishes parsed registry content from deferred FE gameplay systems.
- [x] Extend `tfs-audit` with registry counts, missing-reference diagnostics, and deferred-runtime categories.
- [x] Add self-contained XML registry and CLI audit fixtures without importing private game content.
- [x] Package and publish the TFS registry-inventory release with Linux, Windows, and SHA-256 assets.
- [x] Audit TFS monster and NPC definition XML contracts against existing TFS spawn records.
- [x] Load bounded monster and NPC definition catalogs from safe registry references without executing Lua behavior.
- [x] Cross-validate spawn records against loaded entity catalogs and report unresolved names or missing definitions.
- [x] Extend `tfs-audit` with entity-catalog and spawn-resolution diagnostics.
- [x] Add self-contained monster, NPC, and spawn-resolution fixtures without importing private game content.
- [x] Package and publish the TFS entity-catalog conversion release with Linux, Windows, and SHA-256 assets.
- [x] Audit native FE world and OTCv8 creature-state boundaries for safe static TFS spawn materialization.
- [x] Extract bounded entity identity and appearance metadata from private TFS monster and NPC definitions without loading behavior.
- [x] Build deterministic static FE runtime entities from resolved private TFS spawn records.
- [x] Thread static spawn entities through the native 740 world-initialization contract without claiming AI, combat, or Lua support.
- [x] Add self-contained static-spawn, appearance, and native-map regression fixtures.
- [x] Package and publish the static TFS spawn-runtime foundation release with Linux, Windows, and SHA-256 assets.
- [x] Audit authoritative world-state and native map update boundaries for static creature occupancy.
- [x] Model bounded resolved static creatures in FE world state without adding respawn, AI, combat, movement, or Lua behavior.
- [x] Reject native player movement into occupied static creature tiles while preserving current map and player collision rules.
- [x] Add self-contained authoritative occupancy and native movement regression coverage.
- [x] Package and publish the static creature occupancy foundation release with Linux, Windows, and SHA-256 assets.
- [x] Audit static creature registration, authoritative reset, and native session-boundary behavior.
- [x] Model deterministic static creature lifecycle metadata and safe reset semantics without timer-driven respawning, AI, combat, movement, or Lua execution.
- [x] Integrate static creature lifecycle reset behavior with authoritative native world setup.
- [x] Add self-contained lifecycle registration and reset regression coverage.
- [x] Package and publish the static creature lifecycle policy release with Linux, Windows, and SHA-256 assets.
- [x] Audit authoritative creature movement, occupancy, and native 740 map-update boundaries.
- [x] Implement deterministic server-owned static creature movement with walkability, player occupancy, and creature occupancy safeguards.
- [x] Emit safe native 740 visibility updates for server-driven static creature moves without adding AI behavior.
- [x] Add self-contained authoritative movement and native visibility regression coverage.
- [x] Package and publish the static creature movement foundation release with Linux, Windows, and SHA-256 assets.
- [x] Audit authoritative static creature state, ticking, and native session boundaries for deterministic scheduling.
- [x] Model an opt-in deterministic policy that selects only safe adjacent static creature steps without target selection or pathfinding.
- [x] Integrate bounded scheduled static creature movement with authoritative native map visibility refreshes.
- [x] Add self-contained decision-policy and native update regression coverage.
- [x] Package and publish the deterministic static creature policy release with Linux, Windows, and SHA-256 assets.
- [x] Audit current native session-local world ownership and safe shared-world synchronization boundaries.
- [x] Model a synchronized shared authoritative world host with controlled ticks and bounded player lifecycle operations.
- [x] Integrate native sessions with shared player occupancy and current-world map snapshots.
- [x] Add self-contained shared-world synchronization and concurrent-session regression coverage.
- [x] Package and publish the shared authoritative world host release with Linux, Windows, and SHA-256 assets.
- [x] Audit shared player render state and native 740 creature visibility/update boundaries.
- [x] Expose a bounded authoritative snapshot of renderable active players from the shared world.
- [x] Render other active players in native 740 initial and refreshed map snapshots.
- [x] Deliver safe shared-player join, leave, and movement visibility updates without combat, chat synchronization, AI, or Lua.
- [x] Add self-contained multi-player visibility and concurrent-session regression coverage.
- [x] Package and publish the native multi-player visibility release with Linux, Windows, and SHA-256 assets.
- [x] Audit native 740 chat encoding and shared-session delivery boundaries.
- [x] Model bounded sanitized public-chat events and active-recipient registration without private messages or channels.
- [x] Encode and deliver shared public chat to active native sessions without destabilizing heartbeat or visibility refresh behavior.
- [x] Add self-contained shared chat broadcast and native session regression coverage.
- [x] Package and publish the shared native chat release with Linux, Windows, and SHA-256 assets.
- [x] Audit native 740 target and follow request decoding against shared authoritative player-state boundaries.
- [x] Model bounded shared target and follow intent state with validation and automatic cleanup on target departure.
- [x] Decode and apply supported native target and follow selections without attack resolution or automatic movement.
- [x] Add self-contained interaction-state and native session regression coverage.
- [x] Package and publish the shared interaction-state release with Linux, Windows, and SHA-256 assets.
- [x] Diagnose and correct native 740 movement-facing resets during valid moves while retaining expected finite-map boundary rejection.
- [x] Serialize rapid click-to-walk, stop, and manual movement input so native sessions do not lag or desynchronize under spammed input.
- [x] Safely decode the live native 740 outfit-selection/change records without disconnecting or claiming persistent outfit support.
- [x] Verify against the public OTCv8 legacy outfit parser that the existing native `0xD2` to `0xC8` response uses only the classic current-outfit and one-byte look-range fields; retain real-client confirmation as a blocker.
- [x] Add regression coverage for live-client-informed movement, interruption, and outfit interaction frames.
- [x] Package and publish the native 740 input stability patch release with Linux, Windows, and SHA-256 assets.
- [x] Add an opt-in `run --ed` extended diagnostic mode with bounded native-session, movement, and disconnect metadata while never logging account credentials or packet bodies.
- [x] Replace per-step full viewport movement refreshes that cause visible short-distance teleports during rapid click-to-walk input.
- [x] Encode native 740 shared chat through the stock default console tab rather than the current animated-text fallback.
- [x] Diagnose the native 740 disconnect caused by diagonal numpad movement input.
- [x] Decode and apply bounded authoritative diagonal movement while preserving map bounds, walkability, and occupancy safeguards.
- [x] Add diagonal input codec and native-session regression coverage.
- [x] Package and publish the native 740 diagonal-input patch release with Linux, Windows, and SHA-256 assets.
- [x] Diagnose rapid native 740 click-to-walk frame accumulation that accelerates movement and produces visible tile-by-tile teleporting.
- [x] Replace stale click paths with the latest bounded movement intent while retaining one authoritative step per configured movement interval.
- [x] Add rapid-click path replacement and movement-rate regression coverage.
- [x] Package and publish the native 740 click-path scheduling patch release with Linux, Windows, and SHA-256 assets.
- [x] Audit classic TFS player movement scheduling and cancellation principles against FE’s current native click-walk loop without copying upstream implementation code.
- [x] Replace the current nested native click-path loop with one authoritative active-walk task and a fixed next-step deadline.
- [x] Apply rapid new click paths as latest intent only, without immediate movement, path accumulation, or cadence reset.
- [x] Add deterministic TFS-style rapid-click, replacement, and manual-interruption regression coverage.
- [x] Package and publish the TFS-style native click-walk scheduling patch release with Linux, Windows, and SHA-256 assets.
- [x] Verify FE 7.4.36 repeated native click-path scheduling and default-console shared chat delivery through focused automated protocol/session tests, documenting the remaining live OTClientV8 visual confirmation.
- [x] Audit TFS and classic client 7.4 public-chat packet behavior without copying upstream implementation code.
- [x] Replace the currently broken native 740 shared public-chat encoder with a bounded TFS-compatible record.
- [x] Add end-to-end native chat input, broadcast, and client-record regression coverage beyond a mode-only assertion.
- [x] Package and publish the native 740 chat compatibility patch release with Linux, Windows, and SHA-256 assets.
- [x] Decode the real-client `GameServerTalk` mode-255 failure and identify the actual OTCv8 740 mode-map gap rather than a speech-preamble field defect.
- [x] Correct native 740 shared public-chat delivery so OTCv8 consumes console and spatial feedback without `GameServerTalk` parser errors.
- [x] Add parser-aligned end-to-end shared-chat regressions using the observed real-client failure shape.
- [x] Package and publish the native 740 chat parser-fix release with Linux, Windows, and SHA-256 assets.
- [x] Audit the current `run --ed` native diagnostic output and define bounded, privacy-safe trace categories.
- [x] Add structured extended diagnostics for native session lifecycle, decoded actions, active click-walk state, movement outcomes, chat delivery, map updates, outbound packet metadata, and typed failures.
- [x] Add regression coverage proving diagnostics remain opt-in and do not emit credentials or raw packet bodies.
- [x] Package and publish the extended native diagnostic patch release with Linux, Windows, and SHA-256 assets.
- [x] Replace native 740 `GameServerTalk` delivery, which unmodified OTCv8 rejects because its 740 message-mode map lacks public-say mode translation, with parser-safe no-client-modification delivery.
- [x] Deliver shared public chat through the working default-console status message and a bounded map-visible effect at the authoritative speaker position.
- [x] Add regression coverage proving native 740 shared chat emits no `GameServerTalk` record while retaining console and map-visible delivery.
- [x] Audit FE 7.4 native OTCv8 session transitions, frame decoding, movement scheduling, shared-world refreshes, and disconnect handling for reproducible defects.
- [x] Audit TFS-style configuration, map/content conversion, persistence, and CLI error paths using malformed and boundary-case fixtures.
- [x] Add regression tests and corrective patches for every verified defect discovered by the current bug-hunt.
- [x] Validate, package, and publish the verified FE 7.4 bug-fix release with Linux, Windows, and SHA-256 assets.
- [x] Replace native 740 public-chat output with a parser-accepted, non-animated UI delivery path; remove every outbound record that produces OTCv8 mode 255.
- [x] Implement the native 740 outfit-window response and a safe bounded change-outfit round trip for the operator-supplied client appearance set.
- [x] Add protocol and host regressions for native 740 chat/outfit packets, including absence of unsupported `0xAA` and invalid-mode `0xB4` records.
- [ ] Establish and execute the tested playable-server roadmap: persistent player state, visible health/mana, combat, creatures, inventory/containers, and bounded Lua integration.
- [x] Add persistent player health, mana, experience, and skill-state fields to the authoritative core and SQLite contracts with safe migration behavior.
- [x] Add a versioned SQLite migration and native-session contract for persisted health, mana, capacity, and magic-level values, with backward-compatible defaults for existing worlds.
- [x] Emit parser-safe native 740 player stats and skills from persisted player state instead of placeholder values, with full protocol/persistence/host regressions.
- [x] Define and test a bounded combat resolution foundation that can modify persisted health and drive the native 740 HUD without claiming spell or monster-AI support.
- [x] Add a parser-safe native 740 creature-health update encoder and a bounded selected-player combat path that persists resulting vitals without implementing spells, monsters, or automatic attacks.
- [x] Validate, package, and publish the next parser-safe native 740 compatibility release with Linux, Windows, and SHA-256 assets.
- [x] Validate, package, and publish FE 7.4.42 persistent player-state and bounded combat-foundation assets with Linux, Windows, and SHA-256 checksums.
- [x] Establish a source-controlled per-profile capability matrix that distinguishes implemented behavior from foundations and deferred systems.
- [x] Preserve the current local-first command-line workflow through help-text and option-parsing regression coverage before adding new operator capabilities.
- [x] Report parsed TFS weapon registries as a distinct deferred weapon-runtime dependency during conversion audit output.
- [x] Add a monotonic authoritative world revision for future item, condition, combat, and creature change delivery without replacing the existing visibility/vitals epochs.
- [x] Define bounded typed runtime item instances and equipment slots as the core foundation for persistent inventory and containers.
- [x] Add schema-v3 transactional persistence for player equipment with safe migration and malformed-record rejection.
- [x] Define a bounded ordered core container model for future persistent inventories, depot storage, loot, and nested item trees.
- [x] Add authoritative player-owned equipment state with validated replacement semantics, without claiming native inventory-window delivery.
- [x] Load persisted player equipment into the authoritative native session world during character registration without claiming inventory-window delivery.
- [x] Expose validated OTB server-to-client item ID translation as a prerequisite for parser-safe native inventory delivery.
- [x] Add a profile-gated classic-740 inventory record encoder with golden byte-layout tests, without emitting it from native sessions yet.
- [x] Preserve validated OTB item group and subtype-relevant flags so classic 740 inventory records can derive subtype presence from operator-supplied metadata.
- [x] Add a dependency-neutral validated item-presentation catalog so configuration can pass classic client-ID and subtype requirements to the native host without a crate cycle.
- [x] Pass the validated item-presentation catalog through native host configuration without emitting inventory packets yet.
- [x] Resolve authoritative player items through the validated native host catalog into classic record metadata without emitting packet writes yet.
- [x] Append only catalog-validated classic-740 equipment records to native bootstrap delivery in deterministic multi-slot order, omitting unmapped items without guessed packet output.
- [x] Add a strict classic-740 container-open encoder with golden parser-layout tests and profile-gated login bootstrap for top-level fully mapped persisted containers, without enabling client item-use or container-window actions yet.
- [x] Add additive local player equipment provisioning commands that persist bounded item instances without altering existing player command syntax.
- [x] Define authoritative player-owned named containers with bounded IDs and items as the foundation for persistence and future parser-safe window delivery.
- [x] Add schema-v4 transactional persistence for player-owned named containers and ordered container items with safe migration and malformed-record rejection.
- [x] Hydrate persisted player containers into authoritative native sessions during registration without sending unsolicited container-window packets.
- [x] Add additive local player-container provisioning commands that create named containers and persist bounded container items without altering existing player command syntax.
- [x] Add typed authoritative seven-skill level/percentage state and extensible numeric vocation identity with no-op-safe world replacement semantics.
- [x] Add schema-v5 safe SQLite migration plus validated transactional persistence and legacy defaults for player skills and vocation identity.
- [x] Deliver persisted typed skills through the parser-verified classic-740 bootstrap and dedicated authoritative refresh path.
- [x] Add additive `player vocation` and `player skill` local provisioning commands without changing existing CLI grammar.
- [x] Load and validate operator-owned TFS-style `data/XML/vocations.xml` rules, including promoted/custom vocation entries and bounded gain/multiplier settings, without activating gameplay runtime behavior.
- [ ] Implement formula-based skill tries, magic-level advancement, level progression, and configured vocation multipliers without claiming TFS behavior before profile-specific evidence.
- [x] Add a bounded non-executing parser for TFS `experienceStages` table entries and flat `rateExp`, preserving ordered range precedence and rejecting malformed/overlapping stage data.
- [x] Add a bounded non-executing TFS-style `data/XML/stages.xml` parser with ordered range validation and malformed/overlap rejection; Lua-table syntax and live rate application remain deferred.
- [ ] Add a validated death-loss policy for `deathLosePercent` modes and a town/temple respawn state model before exposing any client-visible death or respawn behavior.
- [x] Add a typed validated death-loss policy model for documented `deathLosePercent` modes without yet applying loss, respawning a player, or claiming a default-formula implementation.
- [x] Add elapsed-time-safe online health/mana regeneration using validated vocation rules, bounded catch-up, persisted vitals, and native 740 HUD refreshes; soul remains deferred.
- [x] Add a bounded authoritative core scheduler for replacement-by-kind poison, burning, and energy damage-over-time conditions with capped damage, expiry cleanup, and lifecycle cleanup; persistence, client effects, and death policy remain deferred.
- [x] Add bounded condition state and profile-gated poison, burning, and energy-damage scheduling.
- [x] Add death, respawn, temple selection, loss policy, and client-state delivery foundations; full client-state delivery remains deferred.
- [x] Expose imported world-map town and temple-position lookup accessors as the validated map-data prerequisite for later temple respawn behavior.
- [x] Add schema-v6 player town assignment persistence with a zero-default migration, authenticated-character hydration, and an additive update helper; temple teleportation and death mechanics remain deferred.
- [x] Add an authoritative in-memory player death-state record that validates a selected imported-town temple and sets health to zero without yet teleporting, persisting death state, applying loss, or sending client death packets.
- [x] Add schema-v7 transactional persistence and strict validated reload for bounded player conditions; preserve no-op safe defaults for older worlds while deferring automatic native-session hydration and client effects.
- [x] Add bounded typed skill-try and magic-mana counters plus explicit multiplier-driven advancement rules derived from the validated vocation registry; weapon/spell gain events and profile-specific parity validation remain deferred.
- [ ] Define and benchmark deterministic multithreaded FE execution boundaries: parallelize independent parsing, content loading, protocol preparation, and safe snapshot work while serializing authoritative world mutations with reproducible ordering and concurrency regressions.
- [x] Add one deterministic parallel native viewport-preparation boundary from immutable render snapshots, while preserving serialized authoritative world mutations and reproducible packet ordering. Verified existing detached native render-snapshot thread regression; broader execution-boundary benchmarking remains open.
- [x] Add a bounded authoritative respawn transition that restores a dead player at the already validated temple; preserve separate deferred work for death-loss execution, respawn persistence, and client-visible death/respawn packets.
- [x] Add a bounded authoritative fixed-percent death-loss transition over exact experience, skill tries, and magic mana using a caller-supplied effective percentage; default-formula, blessing/promotion adjustment, persistence, and client delivery remain deferred.
- [x] Add an additive local `player town` command that persists a character's assigned imported-town ID for later temple-respawn integration, without changing existing command grammar or activating live client death/respawn delivery.
- [x] Hydrate persisted player town assignments into authoritative native shared-world state and clear them on disconnect; runtime death/respawn packet delivery remains deferred.
- [x] Activate the existing authoritative death state when bounded selected-player melee reaches zero health and the target has a validated hydrated town/temple; client death screens, automatic respawn packets, and unverified default loss behavior remain deferred.
- [x] Apply hydrated bounded poison, burning, and energy conditions on the native heartbeat through authoritative vitals transitions; client effect packets, death-state activation from condition damage, and condition persistence after runtime ticks remain deferred.
- [x] Define a repeatable validation matrix for each currently implemented FE 7.4 behavior, separating automated assertions from required unmodified-OTClientV8 manual checks.
- [x] Add a bounded local validation harness for the supported CLI, SQLite migration, configuration, world import, and native protocol smoke-test paths; do not claim production coverage for deferred gameplay systems.
- [x] Document production-release blockers and evidence requirements for a future production-readiness decision.
- [x] Define weighted, evidence-based FE 7.4 completion categories so “more than 50% complete” means implemented, tested runtime scope rather than an unsupported claim.
- [x] Complete the next bounded FE 7.4 progression slice by applying configured experience rates and stages to authoritative experience awards. Verified existing bounded operator award coverage for `rateExp` and optional `stages.xml` ranges; combat, quest, monster, and client reward event sources remain deferred.
- [x] Add a bounded authoritative operator experience award that applies `rateExp` and optional `data/XML/stages.xml` ranges, updates exact level/experience state, and persists it; combat, quest, monster, and client reward event sources remain deferred.
- [ ] Complete the next bounded FE 7.4 melee progression slice by awarding configured vocation-based skill tries without claiming weapon or spell formula support.
- [x] Award one configured vocation-based fist try for each successful fixed selected-player melee hit and persist exact progression counters; weapon and spell formula support remains deferred.
- [ ] Continue FE 7.4 world-interaction, content-runtime, social, and operational milestones with regression coverage and explicit capability reporting until the documented completion model exceeds 50%.
- [x] Award configured vocation-based melee skill tries after accepted native FE 7.4 melee outcomes only when the authoritative progression, persistence, and client-refresh paths are independently covered. Verified existing selected-player melee coverage for rate-scaled configured fist tries, authoritative progression, and SQLite persistence; broader weapon and spell formula behavior remains deferred.
- [x] Publish a weighted FE 7.4 completion ledger with evidence requirements for each system category and a rule for reporting progress above 50%.
- [x] Apply configured experience rates and `stages.xml` ranges to authoritative operator experience awards, with exact regression coverage; combat/quest/monster event sources remain deferred.
- [x] Apply configured vocation progression formulas to fixed-melee fist skill-try awards, with no claim of weapon or spell formulas.
- [ ] Deliver bounded client-visible lifecycle state: condition health updates, death state, temple respawn, and only verified loss policies.
- [x] Emit one parser-shaped native 740 death frame after a persisted lethal condition transition at a validated town temple. A live socket regression verifies the frame and persisted dead/zero-health state; temple respawn delivery and real-client confirmation remain deferred.
- [x] Persist and hydrate authoritative dead/respawn/loss state across restarts without claiming client death screens, automatic timers, teleport packets, or unverified default-loss behavior.
- [x] Persist and hydrate deterministic elapsed progress for bounded poison, burning, and energy schedules across restarts; client effect packets and condition-driven death behavior remain deferred.
- [x] Persist authoritative runtime condition schedule transitions after each native heartbeat so elapsed progress and expired schedules survive restart.
- [x] Activate and persist authoritative death state when bounded poison, burning, or energy damage reaches zero health at a validated town temple; client death screens and condition effects remain deferred.
- [x] Define typed bounded physical adjacent-melee events, validated attack timing, and deterministic player cooldown state as the next combat-event foundation; weapon, spell, PvP, defensive-value, and client-effect behavior remain deferred.
- [x] Define bounded profile-neutral defense ratings and deterministic physical mitigation for typed adjacent melee events; TFS armor, shielding, equipment, weapon, PvP, persistence, and client-effect formula parity remain deferred.
- [x] Parse and validate a bounded declarative scriptless weapon catalog for typed adjacent melee events; TFS Lua weapon scripts, legacy XML formula semantics, equipment binding, and client weapon-use packets remain deferred.
- [x] Bind a validated declarative weapon only to the equipped main-hand item on the existing profile-approved selected-melee path; scripts, legacy formula semantics, map use, and client weapon-use packets remain deferred.
- [x] Define typed bounded spell-cast events with authoritative mana and cooldown enforcement; profile gating, native spell packets, visual effects, Lua formulas, legacy spell semantics, and PvP policy remain deferred.
- [x] Parse and validate a bounded scriptless FE declarative spell catalog that constructs typed spell-cast events; profile selection, client spell requests, formulas, effects, target resolution, and Lua remain deferred.
- [x] Load the optional declarative spell catalog as immutable native-host configuration; client invocation, profile gating, target handling, effects, and Lua remain deferred.
- [x] Resolve a declared spell through immutable host catalog input and apply only authoritative mana/cooldown accounting; client requests, targets, formulas, effects, persistence, and Lua remain deferred.
- [x] Define typed bounded adjacent item-use intents against authoritative map tiles; action execution, doors, switches, containers, Lua, and client item-use packets remain deferred.
- [x] Expose side-effect-free authoritative map-item validation through the synchronized host boundary; client routing, action execution, doors, switches, containers, Lua, and persistence remain deferred.
- [x] Advance the native authoritative world tick from elapsed heartbeat time so lifecycle timestamps and future combat cooldowns share one deterministic server clock; client packet semantics remain unchanged.
- [ ] Add profile-driven combat events for weapons, spells, damage types, cooldowns, PvP policy, and death/corpse handling after independent compatibility evidence.
- [ ] Add authoritative item transfer, equipment/inventory synchronization, container windows, item use, actions, doors, switches, and map interactions.
- [x] Add bounded authoritative transfer between one player equipment slot and one already owned container; client inventory packets, map-ground transfers, nested containers, stack splitting, and item-use semantics remain deferred.
- [ ] Add validated monster/NPC runtime behavior, spawns, respawns, combat participation, loot, corpses, and script-event dispatch boundaries.
- [x] Expose bounded authoritative reset of inactive imported static entities with native visibility refresh; legacy per-spawn intervals now propagate into deterministic due-reactivation state, one native host heartbeat owns shared-world tick advancement and visibility refresh, active static entities accept native target selection only, and bounded server-owned display health is delivered during native login and visibility refresh, while monster/NPC AI, combat, loot, corpses, and scripts remain deferred.
- [x] Re-arm one complete validated per-spawn delay after a due static-creature reactivation is blocked by player or active-static occupancy. The bounded deterministic retry mirrors the observed TFS failed-spawn cadence without adding chance rolls, spawn pools, spectators, autonomous scheduling, AI, or scripts.
- [ ] Add sandboxed Lua execution with safe event dispatch, legacy registry adapters, and deterministic failure handling.
- [x] Add one VM-local bounded TFS-style Lua `table.create(arrayCapacity, recordCapacity)` helper to the fresh no-standard-library expression and callback sandboxes. Both capacities are capped at `256`; tables remain local and cannot cross the primitive-only boundary. Filesystem, network, process, package, debug, host APIs, TFS userdata, module loading, and gameplay mutation remain unavailable.
- [x] Add one VM-local bounded TFS-style Lua `table.pack(...)` helper to the fresh no-standard-library expression and callback sandboxes. It preserves the argument count in `n`, including nil entries, and rejects more than `256` arguments. Tables remain local; filesystem, network, process, package, debug, host APIs, TFS userdata, module loading, and gameplay mutation remain unavailable.
- [ ] Add guilds, parties, channels, private messages, depots, banking/trading, houses, access lists, and administrator controls.
- [x] Decode the exact zero-payload classic 740 channel-list request and return a parser-backed empty `0xAB` channel list. Channel definitions, joining, leaving, membership, messages, moderation, persistence, guild/party integration, scripts, and real-client confirmation remain deferred.
- [x] Load only validated operator-supplied public entries from standard `data/chatchannels/chatchannels.xml` and deliver them through the existing native 740 channel-list response. Script attributes, non-public channels, membership, joins, messages, moderation, persistence, guild/party integration, and real-client confirmation remain deferred.
- [x] Generate an empty standard `data/chatchannels/chatchannels.xml` root for new FE worlds without overwriting an operator’s existing TFS-compatible channel catalog during later skeleton reconciliation.
- [x] Decode exact classic 740 Join Channel requests and open only a matching configured public channel with empty player and invitation lists. FE does not retain membership or enable channel messages, private/guild/party channels, moderation, persistence, scripts, or real-client confirmation.
- [x] Retain exact classic 740 Talk mode and channel ID fields, then route only configured public-channel messages through parser-backed channel Talk records. Membership, leave handling, private/guild/party channels, moderation, persistence, scripts, flood limits, and real-client confirmation must remain deferred.
- [x] Decode exact classic 740 Leave Channel requests and remove only that ID from the live session’s configured public-channel set. No persistent membership, channel close record, permissions, message history, moderation, scripts, or real-client confirmation may be claimed.
- [ ] Add release-blocking load, soak, memory, fault-recovery, security, real-client interoperability, Windows, and migration validation evidence.
- [x] Fix the native 740 player-stats (`0xA0`) record layout that the real client reports as end-of-input after the initial map payload; the parser-shaped record now has a 16-bit level and soul byte.
- [x] Audit the reported `12800` native mana display against persistence and public OTCv8 740 field conditions; add a byte-level regression proving persisted `42 / 50` mana remains distinct from `32,000` capacity after the corrected player-stats layout.
- [ ] Verify the corrected native 740 player-stats record with an unmodified OTClientV8 build; do not clear the real-client release blocker until the initial payload parses without an EOF error.
- [x] Correct authoritative player HUD initialization and refresh so level, experience, mana, maximum mana, and magic level match persisted server state after login. Verified existing authoritative snapshot hydration and byte-level persisted mana/capacity regression coverage; unmodified-client confirmation remains a separate release blocker.
- [x] Persist the authoritative player position on supported movement and restore it after orderly or abrupt native-session relog rather than returning the character to the default spawn.
- [ ] Verify native 740 position persistence with an unmodified OTClientV8 build: move, exit normally, relog, and confirm the real client begins at the saved tile without a parser error or disconnect.
- [x] Persist accepted matching native 740 player outfit changes in schema-v11 and hydrate them on the next session with a configured look-type fallback.
- [ ] Verify native 740 outfit persistence with an unmodified OTClientV8 build: change accepted colors, relog, and confirm the stored appearance is visible without a parser error or disconnect.
- [x] Implement or safely acknowledge profile-verified native 740 look/inspect actions for map things and creatures. Verified existing parser-backed authoritative LookMap and visible LookCreature status-text delivery with socket coverage; unmodified-client confirmation remains separate.
- [x] Implement a verified native 740 public-chat message layout. The profile now emits a parser-backed `0xAA` `Say` record with mode `1`, authoritative speaker position, and bounded sanitized text; direct socket coverage verifies the complete frame while other social modes remain deferred.
- [x] Confirm through public OTCv8 issue #218 that unmodified clients do not construct a message-mode map below protocol 760; retain safe native 740 visible-chat suppression rather than emitting an invalid server-side mode byte.
- [x] Decode and safely handle the native 740 Quest Log request without ending the game session; until content exists it returns a parser-shaped empty/deferred response.
- [ ] Verify the native 740 empty Quest Log response with an unmodified OTClientV8 build; do not clear the client-facing blocker until the window opens without a parser error or disconnect.
- [x] Build, package, checksum, and publish fresh Linux and Windows artifacts from the latest verified Forgotten Engine commit for manual testing.
- [x] Build and verify unpublished local FE 7.4.44 Linux and Windows release-candidate archives from commit `7348d4d`, including SHA-256 manifests and ZIP integrity; Windows runtime smoke testing and GitHub publication remain blocked or deferred.
- [ ] Establish and satisfy production-readiness gates for complete declared TFS feature conformance, profile-specific protocol validation, reliability, bounded memory/performance, security, and reproducible TFS-to-FE migration before describing any profile as mainstream-ready.
- [x] Expand the completion ledger from its interim 51% target into a full evidence-based 100% production-readiness plan covering every declared FE system, compatibility profile, migration boundary, and operational gate.
- [x] Define a typed audit-derived deferred script-event interface and no-op executor that never reads, loads, or runs imported Lua; real sandboxing, legacy APIs, and gameplay event execution remain deferred.
- [x] Define an explicit FE 8.0 native-profile foundation that reports its RSA/XTEA transport requirement and remains non-runnable until parser-backed encrypted session support exists; no listener or protocol-parity claim is enabled.
- [x] Add bounded server-side selected melee against an active targeted static creature using percentage-point health only; deactivation may refresh native visibility, while TFS combat formulas, AI, loot, corpses, scripts, and persistent monster state remain deferred.
- [x] Add bounded operator-configured native 7.4 outfit start/end look-type fields for the classic dialog; require an inclusive valid range around the configured current look type without validating or distributing client assets.
- [x] Persist a concrete native classic outfit change only when its look type falls within the configured inclusive range; reject out-of-range changes without altering stored appearance or claiming asset/addon/mount support.
- [x] Reuse the existing one-tick player combat cooldown for bounded native static-creature melee so repeated input cannot accelerate fixed percentage damage; weapon timing, formulas, mitigation, rewards, AI, loot, corpses, and scripts remain deferred.
- [x] Add a bounded pure FE 8.0 RSA/XTEA transport-envelope primitive for future parser-backed session work; it must not decrypt client input, parse protocol-800 messages, enable a listener, or expose key material in diagnostics.
- [x] Add a marker-and-key-only FE 8.0 bootstrap context parser over an already decrypted fixed RSA block; it must not parse credentials or other login fields, accept live input, start a listener, or expose key material in diagnostics.
- [x] Parse and retain bounded legacy vocation `gainhp`, `gainmana`, and `gaincap` metadata as immutable configuration only; do not calculate levels, mutate player vitals/capacity, or change native HUD output.
- [x] Replace the MVP experience-level resolver in existing authoritative experience awards and loss with a bounded overflow-safe classic threshold calculation; defer vocation gains, vitals/capacity mutation, and native HUD advancement output.
- [x] Maintain a source-aided TFS-to-FE subsystem matrix from the official public TFS repository; use it as a feature and behavior inventory, implement each accepted slice independently in Rust, and retain explicit tests and deferred boundaries.
- [x] Add an additive authoritative vocation-aware level-up transaction that applies parsed `gainhp`, `gainmana`, and `gaincap` to current/max vitals and capacity after a real experience-level increase; persist it atomically for the stable CLI while deferring native HUD delivery and full formula parity.
- [x] Emit a profile-verified native 740 player-stats refresh after an authoritative level-up transaction, sourced from current synchronized state; retain the real-client `0xA0` parser confirmation blocker and do not claim full advancement delivery.
- [x] Synchronize native 740 equipment changes from authoritative mapped player equipment through parser-verified set/delete inventory records; retain omission of unmapped items and defer item transfer, container, stack, and real-client confirmation behavior.
- [x] Add bounded authoritative item-location and transfer foundations across existing equipment and owned top-level containers, with duplicate ownership prevention and atomic validation; defer client item requests, ground surfaces, stacking, capacity, recursive nesting, and real-client behavior.
- [x] Extend deterministic static-creature target selection and movement boundaries with server-owned, map-validated state transitions and explicit reset behavior; defer full AI, pathfinding, dynamic combat, loot, NPCs, and real-client creature parity.
- [x] Add a bounded sandboxed scripting execution slice with strict source/resource limits, deterministic typed result handling, and no filesystem, network, process, host mutation, or legacy API exposure; retain full Lua compatibility and Plugin SDK work as deferred.
- [x] Add one opt-in deterministic target-directed cardinal step for an already selected static-creature target, reusing map and occupancy validation; defer pursuit loops, obstacle pathfinding, target acquisition scheduling, AI, combat automation, and client behavior.
- [x] Route a caller-triggered bounded static-creature target step through the shared native-world visibility refresh path, reusing established native viewport records while deferring autonomous scheduling, AI, client target semantics, and real-client confirmation.
- [x] Synchronize complete catalog-mapped top-level container replacements through the shared native-world epoch and existing native container-open record; defer client container actions, recursive containers, incremental item deltas, item transfer routing, and real-client confirmation.
- [x] Route existing bounded complete-item equipment/container transfers through the shared world and affected native refresh epochs; defer client requests, persistence routing, stacks, swaps, ground transfer, slot compatibility, recursive containers, and real-client confirmation.
- [x] Add an opt-in shared-heartbeat static-creature target acquisition policy with a bounded range and deterministic ordering; defer pursuit, autonomous movement/combat, client target delivery, scripts, and real-client confirmation.
- [x] Add an opt-in one-step static pursuit policy that acquires a bounded nearest target then attempts one legal cardinal move per active static creature; defer default scheduling, multi-step routing, obstacle pathfinding, combat, scripts, client target delivery, and real-client confirmation.
- [x] Persist and restore a bounded static-creature runtime snapshot for known catalog/static-spawn IDs, covering only position, active state, and bounded health; defer target state, autonomous scheduling, combat, loot, dynamic spawns, and real-client confirmation.
- [x] Add bounded authoritative item-stack split and merge semantics for existing equipment/container transfers; defer item metadata-driven stack rules, capacity, ownership, ground transfer, nested containers, slot compatibility, client requests, and real-client confirmation.
- [x] Add a bounded trusted-Lua callback dispatcher with typed registration and sandboxed invocation; defer world mutation APIs, filesystem/network access, arbitrary module loading, legacy TFS Lua surface compatibility, event discovery, and real-server confirmation.
- [x] Add additive stable CLI commands for bounded persisted equipment/container stack transfers; preserve current commands and defer client-originated inventory requests, map-ground transfer, nested containers, slot compatibility, and real-client confirmation.
- [x] Persist and restore bounded static-creature reactivation scheduling for known static IDs; defer target state, autonomous AI cadence, combat formulas, loot, dynamic spawns, and real-client confirmation.
- [x] Add bounded authoritative magic-mana progression awards using configured vocation multipliers, exact threshold accounting, persistence, and additive stable CLI access; defer spell-client requests, Lua formulas, full TFS rate parity, and real-client confirmation.
- [x] Parallelize deterministic loading of independent operator-owned content catalogs while preserving a fixed validation/error order and serialized authoritative world mutation; defer parallel gameplay mutation, timing-sensitive AI, and performance parity claims.
- [x] Persist authoritative player vitals and exact progression attempts atomically for magic-mana awards; defer live spell-client requests, Lua formulas, full rate parity, and real-client confirmation.
- [x] Add bounded authoritative skill-try awards using configured vocation multipliers, atomic visible-progress/exact-try persistence, and additive stable CLI access; defer live combat inputs, Lua formulas, full rate parity, and real-client confirmation.
- [x] Audit and correct native 740 player HUD initialization/refresh so level, experience, mana, maximum mana, capacity, and magic level are emitted from one persisted authoritative state; retain unmodified-client confirmation as a release blocker.
- [x] Parse bounded global skill and magic progression rates from TFS-style configuration and apply them to deterministic CLI award entry points; native fixed-melee routing and complete TFS formula, weapon, spell, Lua, and real-client parity remain deferred.
- [x] Route the bounded `rateSkill` multiplier through native fixed selected-player melee fist-try awards with persistence and regressions; defer weapon timing, full formulas, spell/Lua sources, and real-client confirmation.
- [x] Add a profile-gated native 740 zero-payload death notification from existing authoritative death transitions; defer automatic respawn scheduling, position-refresh delivery, loss formulas, and real-client confirmation.
- [x] Add bounded path-safe loading of explicit callback-function Lua files into the existing sandbox dispatcher; ordinary TFS scripts, module loading, legacy APIs, and world mutation remain deferred.
- [x] Add a bounded core-only static-creature adjacent attack transition against its selected living target; automatic scheduling, persistence, native delivery, formulas, loot, corpses, scripts, and full AI parity remain deferred.
- [x] Expose the bounded static-creature target attack through the shared-world lock and advance vitals refresh only after real damage; scheduling, persistence, native delivery, formulas, loot, corpses, scripts, and full AI parity remain deferred.
- [x] Decode bounded native 740 map and creature look requests into typed actions without emitting an unverified client-visible response; text-message delivery remains blocked by the unmodified OTCv8 740 message-mode gap.
- [x] Add an opt-in disabled-by-default native static-creature target-attack policy that reuses the bounded shared-world attack transition; formulas, persistence, packets, loot, corpses, scripts, and full monster AI parity remain deferred.
- [x] Persist authoritative player-vitals changes caused by the opt-in native static-creature target-attack policy; client combat effects, loot, corpses, formulas, scripts, and full monster AI parity remain deferred.
- [x] Add an explicit bounded `script dispatch` bridge that resolves one declared safe TFS XML `script` path into the existing callback sandbox; automatic discovery, ordinary registry execution, TFS APIs, module loading, gameplay mutation, and full Lua compatibility remain deferred.
- [x] Deliver the existing evidence-backed native 740 zero-payload death record once when a connected player’s authoritative shared-world state transitions to dead through an opt-in static-creature heartbeat attack; respawn, teleport, timers, loss, combat effects, and real-client confirmation remain deferred.
- [x] Decode native 740 UseItem input into bounded typed fields and route only uniquely catalog-mapped requests through existing side-effect-free server-owned map-item validation; actions, doors, switches, containers, charges, Lua, persistence, messages/effects, and full client behavior remain deferred.
- [x] Add an additive operator-controlled `player respawn` command that applies the existing validated temple-respawn transition and atomically persists restored position, vitals, and cleared lifecycle state; automatic native respawn packets, timers, teleport effects, loss policy, and real-client confirmation remain deferred.
- [x] Reject authoritative player movement while persisted lifecycle state is dead, preserving position and revision until the existing validated respawn transition restores living state; client death/respawn screens, timers, teleport effects, and real-client confirmation remain deferred.
- [x] Reject supported player-initiated damage and mana/cooldown-only spell mutations while authoritative lifecycle state is dead; observation, operator recovery, complete gameplay action coverage, and client lifecycle delivery remain deferred.
- [x] Prevent background regeneration from mutating vitals or schedule state while authoritative lifecycle state is dead, so only the existing validated respawn transition can restore living vitals; client lifecycle delivery remains deferred.
- [x] Apply explicit configured `deathLosePercent` fixed values after supported native player-melee, condition, and static-creature heartbeat deaths, with one atomic SQLite snapshot for level, experience, vitals, skills, exact attempts, and lifecycle state; retain default formula, blessing, promotion, and client loss delivery as deferred.
- [x] Parse and validate bounded legacy item armor, defense, extra-defense, attack-speed, and weapon-type metadata for future profile-specific mitigation without changing live combat behavior or claiming formula compatibility.
- [x] Parse and validate bounded TFS vocation nested-formula defense and armor multipliers for future profile-specific mitigation without changing live combat behavior or claiming formula compatibility.
- [x] Decode and retain bounded native fight-mode, chase-mode, and secure-mode state with the verified TFS defaults, without connecting it to unverified combat formulas or client output.
- [x] Parse and validate bounded TFS vocation attack-speed and base-speed metadata for future timing compatibility without changing movement, cooldown, or combat execution.
- [x] Encode and deliver the verified three-byte native 740 player-modes record from authoritative fight-mode state at initialization and after a changed accepted mode request, while leaving persistence, later PvP-mode variants, and formula effects deferred.
- [x] Decode and honor native 740 CloseContainer as a client-session view closure, emitting the verified close record and preventing container refreshes from reopening that view; persistence, nesting, up-arrow, and item-delta controls remain deferred.
- [x] Decode and honor native 740 UpdateContainer as a session-view refresh for one complete mapped top-level container, without changing shared or persisted state; nesting, up-arrow, and item-delta controls remain deferred.
- [x] Deliver the existing parser-verified native 740 player-modes record immediately after an accepted authoritative fight-mode change; formula effects, persistence, and broader-profile variants remain deferred.
- [x] Correct local player creation so persisted starting experience is consistent with the selected initial level and later authoritative experience awards do not reduce the character level.
- [x] Extend stable `player create` with an optional validated numeric vocation ID, preserving the existing account/name invocation and default vocation when absent.
- [x] Deliver bounded native selected-static-creature melee health updates and persist changed static runtime state, while leaving loot, corpses, formulas, AI, and real-client confirmation deferred.
- [x] Parse bounded monster experience metadata from TFS entity definitions without executing XML or activating reward gameplay; malformed or overflowing values are rejected and NPC metadata remains zero.
- [x] Route parsed monster experience through a deterministic authoritative selected-static defeat reward with existing rate/stage and SQLite progression persistence; vocation vital gains, loot, corpses, formulas, AI, scripts, and real-client confirmation remain deferred.
- [x] Apply configured vocation health, mana, and capacity gains to a real native selected-static defeat level-up, atomically persisting the changed level, experience, and vitals.
- [x] Restore persisted active conditions into a new native shared-world registration after disconnect/relog, preserving validated remaining timing and subsequent vital changes.
- [x] Decode native 740 UseItemEx and validate both catalog-mapped authoritative map-item targets without action execution, mutation, Lua, or outbound packet behavior.
- [x] Decode native 740 battle-window item input and validate one catalog-mapped authoritative map item plus one active authoritative creature target without action execution, combat, mutation, Lua, persistence, or outbound packet behavior.
- [x] Decode native 740 rotate-item input and validate one catalog-mapped authoritative map item without item rotation, mutation, Lua, persistence, or outbound packet behavior.
- [x] Reply to native 740 LookMap only with a verified `0xB4/0x15` bounded status text derived from an exact validated authoritative map item; defer LookCreature and all unverified inspection output.
- [x] Reply to native 740 LookCreature only with verified `0xB4/0x15` bounded status text for a visible authoritative player or active static creature; defer off-screen, inactive, descriptions, attributes, and all broader inspection output.
- [x] Activate an exact authoritative native 740 map teleport item only when its OTBM destination is walkable and unoccupied, atomically persist player position, and send an existing full viewport refresh; defer scripts, effects, generic map actions, and all other item behavior.
- [x] Apply parsed vocation `gainHPTicks` and `gainManaTicks` intervals to bounded live native regeneration, with persistence, regression coverage, and explicit deferral of soul and full TFS timing parity. Verified existing CLI-to-host wiring, heartbeat selection by vocation, atomic vital persistence, and core/host regression coverage.
- [x] Decode and acknowledge bounded native 740 container up-arrow input as a session-only view operation, without nested inventory mutation, persistence changes, or unsupported container traversal semantics.
- [x] Establish a parser-verified native 740 public-chat delivery record for unmodified OTClientV8, or retain explicit safe suppression when public evidence cannot verify a message mode or alternative layout. Public OTCv8 issue #218 confirms no message-mode map below protocol 760 for both speech and text messages, so FE retains its existing safe suppression for unmodified 740 clients.
- [x] Deliver bounded native 740 text-item content only for an exact validated imported map item with safe text metadata, using a parser-verified classic text-window packet; defer editing, writing, scripts, and generic item-action semantics.
- [x] Activate an imported map teleport destination after a validated native player step onto that item, with destination revalidation, authoritative final-position persistence, and a full viewport refresh; defer effects, chains, scripts, and generic tile actions.
- [x] Extend native 740 LookMap only with a bounded imported OTBM description from the exact validated map item, delivered through the existing status-message record; defer generated names, attributes, text content, weight, and generic inspection parity.
- [x] Activate one imported teleport destination after a validated native diagonal move lands on its tile, with the same final-position persistence and full viewport refresh; defer chains, effects, scripts, and generic tile actions.
- [x] Add a parser-verified native 740 refresh for an already open player container only when an existing bounded container-view request proves safe; defer item movement, pagination, nested traversal, persistence mutation, and unsupported container semantics. Verified existing `UpdateContainer` routing, authoritative open-container re-encoding, and socket regression coverage.
- [x] Verify native 740 login HUD hydration against persisted level, experience, health, mana, maximum mana, capacity, and magic level; implement only any parser-verified gap and preserve deferred real-client confirmation. Verified existing authoritative registration, pre-initialization snapshot hydration, and exact host stats-record regression coverage.
- [x] Open one exact validated imported map item with direct children as a bounded read-only native 740 container view, if its parser evidence and data model support it; defer transfers, nested traversal, persistence mutation, and generic container semantics. Audited: imported map items retain children but not authoritative container capacity/type metadata, so FE intentionally does not guess a client container representation.
- [x] Resolve bounded chains of imported map teleports after one supported native trigger, with cycle and hop guards, one final persisted position, and one full viewport refresh; defer effects, scripts, and generic tile behavior.
- [x] Add a live native socket regression for a repeated imported teleport destination, proving the bounded chain terminates at the last successful hop with one final persistence update and viewport refresh.
- [x] Verify native 740 HUD stats refresh delivery after bounded condition damage and vocation regeneration, with exact persisted vital and packet assertions; defer client condition-effect visuals and real-client confirmation. Verified existing vital-epoch advancement, SQLite persistence, authoritative snapshot rehydration, and classic stats-frame delivery in the native heartbeat.
- [x] Emit a parser-verified native 740 creature-outfit update after an accepted persisted player outfit change, while retaining current color/look validation and deferring broader appearance features. Verified existing accepted-change persistence and immediate classic `0x8E` creature-outfit delivery.
- [x] Propagate accepted native 740 player outfit changes to visible peer sessions from authoritative shared player state, while retaining current self-update, persistence, and bounded appearance validation. Shared-state and two-client socket regressions verify the accepted classic look type and four colour bytes; addons, mounts, and real-client confirmation remain deferred.
- [x] Verify the parser-shaped native 740 empty Quest Log response through a live socket and prove the session remains usable afterward; quest storage, mission lines, scripting, and real-client confirmation remain deferred. The exact zero-count response and a following outfit-window request are covered by a native socket regression.
- [x] Propagate validated native 740 outfit head, body, legs, and feet colours to visible peer sessions through authoritative shared appearance state and exact map-render regressions.
- [x] Audit the public OTCv8 740 PlayerStats parser and feature thresholds against FE's native `0xA0` record. The existing 21-byte classic layout matches the selected parser path; unmodified-client EOF-free observation remains an open release blocker.
- [x] Propagate native 740 player facing direction to visible peer sessions from shared authoritative state after accepted turns and movement, with exact map-render regression coverage. Direct shared-state and two-client socket tests cover east-facing turn delivery; broader client animation and real-client confirmation remain deferred.
- [x] Prove through the native two-client socket regression that an accepted cardinal movement updates the visible peer’s direction byte as well as its position. The shared-state position assertion and peer full-map west-facing assertion are both covered.
- [x] Render authoritative native 740 visible-player health percentages for peers and refresh their viewport only after a real shared-vital change, with direct and two-client regression coverage. A bounded 75/150 damage transition is asserted as 50% in the peer map; health-delta/effect packets, broad combat delivery, and real-client confirmation remain deferred.
- [x] Audit and implement bounded parser-verified native 740 authoritative equipment-transfer slices: whole-item fixed-slot-to-empty-slot, fixed-slot-to-owned-top-level-container, owned-top-level-container-to-equipment, and distinct occupied fixed-slot-to-occupied-fixed-slot exchange. Each route validates the classic position, source identity, and explicit bounded count; the exchange requires the complete source item, persists authoritative state, and advances only the required refresh epochs. Generic inventory, nested containers, ground items, capacity, slot compatibility, generic cancellation, scripts, and full `internalMoveItem` parity remain deferred.
- [x] Audit and implement only a parser-verified native 740 whole-item equipment-to-top-level-container transfer slice, retaining reverse moves, nested containers, stacks, ground items, capacity policy, and ownership rules as deferred. A live socket regression verifies authoritative SQLite persistence, `DeleteInventory`, and the refreshed top-level container frame.
- [x] Audit and implement only a parser-verified native 740 whole-item top-level-container-to-empty-equipment transfer slice, retaining swaps, nested containers, stacks, ground items, capacity policy, and ownership rules as deferred. A live socket regression verifies authoritative SQLite persistence, `SetInventory`, and the refreshed empty top-level container frame.
- [x] Audit and implement only a parser-verified native 740 temple-respawn delivery slice, retaining death-screen timing, loss records, automatic timers, generic teleport effects, and broader lifecycle semantics as deferred. A live socket regression verifies that a persisted-dead character respawns at its validated temple before the next native full-map bootstrap.
- [x] Audit and implement one independently evidenced profile-driven native combat-event slice: classic 740 `ClientAttack` with a zero creature ID clears the bounded authoritative target and emits the parser-verified zero-payload `ClearTarget` record. A live socket regression covers the complete request/response. Damage formulas, attack timing, target authorization, effects, loot, PvP, scripts, and broader combat semantics remain deferred.
- [x] Decode and apply the parser-verified native 740 cancel-attack/follow control (`0xBE`) through existing authoritative target/follow clearing, without adding combat formulas, effects, or target rules. Protocol and host regressions cover strict zero-payload decoding and clearing both intents.
- [x] Audit and implement one safe, typed Lua callback-dispatch boundary. An explicit in-memory or canonical-root callback registration is capped by callback count, name, source, VM-memory, and instruction limits; each invocation uses a fresh no-standard-library VM and accepts only a nonempty 64-byte event label, a subject ID that fits Lua’s signed integer range, and one numeric value. Invalid input is rejected before execution and only primitive results cross back. Automatic legacy registry execution, TFS APIs, modules, filesystem/network/process access, persistent globals, and gameplay mutation remain deferred.
- [ ] Audit and implement only one deterministic data-backed static-creature lifecycle slice, retaining full monster AI, loot, formulas, scripts, and NPC behavior as deferred. Blocked pending typed spawn-zone, spectator, spawn-flag, rate-cap, and event-hook contracts: the TFS reference does more than tile-occupancy reactivation.
- [x] Audit and benchmark one deterministic multithreaded execution boundary while preserving serialized authoritative mutations. The bounded 1–8 worker native render-publication pool accepts only detached immutable viewport inputs, caps each worker queue at 32 and a batch at 256, rejects duplicate publication sequences, sorts outputs by caller-provided sequence, and has no world, database, socket, action queue, or mutation callback. Regressions cover byte-identical ordered output, detached snapshots, worker bounds, duplicate rejection, and batch capacity. A local release benchmark found the three-worker pool 8.633% slower than direct preparation for fixed ordered three-publication batches, so it remains disconnected from the listener and makes no performance claim.
- [x] Build a bounded typed deterministic command-batch foundation for future worker handoff, without integrating current authoritative gameplay mutation onto worker threads. Core regressions verify stable `(tick, player_id, session_sequence)` ordering, duplicate-key rejection, capacity bounds, and caller-synchronized handoff.
- [x] Audit and implement only a parser-verified native 740 clear-target delivery when the current player’s bounded selected static target is deactivated, retaining generic combat cancellation, loot, corpses, effects, and AI as deferred. Focused regressions verify no control frame on a nonlethal hit and one classic zero-payload `ClearTarget` frame on deactivation.
- [ ] Audit and implement only a parser-verified native 740 target cleanup for the attacker when FE’s bounded player-death transition invalidates that player target, retaining generic PvP, death, loss, effects, and combat semantics as deferred. Blocked pending typed creature-disappearance ownership: FE’s current authoritative death transition preserves other players’ intents, while the TFS reference resolves cancellation through broader creature lifecycle behavior.
- [x] Audit and implement only a parser-verified native 740 rejection of selecting or following an authoritative dead player, retaining PvP authorization, target cleanup, death delivery, and generic combat semantics as deferred. Core and host regressions verify that both requests are rejected without mutating the source interaction intent.
- [x] Audit and implement only a parser-verified native 740 target-cancellation feedback record for invalidated target selection, retaining follow movement, generic combat feedback, effects, formulas, and client target policy as deferred. A live socket regression verifies the zero-payload classic `ClearTarget` response for an unknown nonzero target.
- [ ] Audit and implement only a parser-verified native 740 target-availability rule using authoritative visibility, retaining combat range, protection, chase, formulas, effects, PvP, and full target policy as deferred. Blocked pending a typed authorization model: the TFS reference couples selection to protection zones, PvP rules, flags, secure mode, attackability, and events, so viewport membership alone would be misleading.
- [x] Decode the parser-verified native 740 classic item-throw request (`0x78`) as an exact 14-byte safe deferred interaction. Authoritative equipment transfer, containers, ground items, stacks, capacity, ownership, persistence coordination, and inventory deltas remain open.
- [x] Implement a validated native 740 equipment-slot-to-empty-slot transfer only for exact classic inventory positions, with client-item identity validation, SQLite persistence, inventory delta frames, and explicit deferral of swaps, containers, ground items, partial stacks, capacity, and ownership policy. A live socket regression covers the right-hand-to-left-hand whole-item path.
- [x] Audit the native 740 occupied-equipment-slot path against the classic throw packet and existing FE equipment model. The packet boundary is established, but FE lacks verified slot compatibility, swap, cancellation, and broader item-transfer contracts.
- [x] Implement a bounded native 740 whole-item top-level-container-to-occupied-equipment-slot swap. The core exchanges cloned authoritative values atomically; the shared route advances both refresh epochs, persists both collections, and is taken only when the request count equals the complete source item count. Slot compatibility, partial swaps, stack swaps, capacity, nested containers, ground items, generic cancellation, scripts, and broader item-move parity remain deferred.
- [x] Audit and implement only a bounded native 740 whole-item top-level-container-to-occupied-equipment-slot swap. The exact source item count must be moved; FE exchanges cloned authoritative values, persists both collections, and advances refresh epochs. Slot compatibility, partial and stack swaps, nested containers, ground items, capacity, generic cancellation, move events, scripts, and TFS `internalMoveItem` parity remain deferred.
- [x] Audit and implement only a parser-verified native 740 matching equipment-stack-to-owned-top-level-container-item merge, retaining generic stacks, swaps, slots, containers, ground items, capacity, and inventory policy as deferred. A live socket regression verifies a 10-count merge, SQLite persistence, `SetInventory`, and the refreshed container record.
- [x] Audit and implement only a parser-verified native 740 matching owned-top-level-container-stack-to-fixed-equipment merge, retaining generic stacks, swaps, slots, containers, ground items, capacity, and inventory policy as deferred. A live socket regression verifies a 10-count merge, SQLite persistence, `SetInventory`, and the refreshed container record.
- [x] Audit and implement only a parser-verified native 740 full matching equipment-stack-to-owned-top-level-container-item merge, retaining generic stacks, swaps, slots, containers, ground items, capacity, and inventory policy as deferred. The existing live socket regression now verifies a full 15-count merge, SQLite persistence, `DeleteInventory`, and the refreshed container record.
- [x] Audit and implement only a parser-verified native 740 full matching owned-top-level-container-stack-to-fixed-equipment merge, retaining generic stacks, swaps, slots, containers, ground items, capacity, and inventory policy as deferred. The existing live socket regression now verifies a full 30-count merge, SQLite persistence, `SetInventory`, and the refreshed empty container record.
- [x] Audit native 740 public-chat delivery against the unmodified OTCv8 parser and implement only a safe parser-verified client-visible status path if one exists; otherwise retain public chat as explicitly deferred. The audited parser has no 740 message-mode map, so no safe client-visible path exists.
- [x] Audit completed: the unmodified OTCv8 message-mode map has no 740 branch, while FE's parser-verified `0xB4/0x15` record is status text rather than public speech. Native 740 public-chat output remains intentionally suppressed; `docs/protocol-evidence/native-740-public-chat.md` records the source-level evidence and required proof for any future implementation.
- [x] Audit and, only where independently supported, add a bounded profile-driven native 740 weapon combat event that applies existing catalog metadata without widening unverified TFS combat semantics. The existing FE declarative equipped-weapon event is already the safe boundary; imported legacy item metadata remains non-live conversion metadata because the required profile timing, formula, class, skill, modifier, and delivery evidence is absent. See `docs/protocol-evidence/profiled-weapon-combat-audit.md`.
- [x] Audit the existing sandboxed Lua callback boundary against validated TFS registry metadata and implement only a bounded, non-automatic legacy registry adapter if its safety contract can be proven. The stable explicit `script dispatch` command already validates a declared registry file and loads it through the restricted callback sandbox; automatic imported registry execution, TFS APIs, module loading, and gameplay mutation remain deferred. See `docs/protocol-evidence/lua-registry-adapter-audit.md`.
- [x] Audit and benchmark one deterministic multithreaded execution boundary, preserving serialized authoritative mutations, bounded memory, reproducible packet order, and concurrency regressions. Added a bounded immutable native render-preparation worker with a 32-request queue and a byte-identical detached-snapshot regression. It has no `SharedNativeWorld`, database, socket, or mutation access and is not yet integrated into the live listener; no performance claim is made pending the documented benchmark gates.
- [x] Build and upload a verified Windows `.exe` and SHA-256 checksum to the existing GitHub release tag `fe-v7.4.44`.
You asked for the Windows release artifact to be attached to the existing `fe-v7.4.44` GitHub release.
- [x] Add a reproducible native viewport-preparation benchmark comparing direct single-threaded encoding with the bounded immutable render worker; record raw samples, method, CPU context, and non-claim limitations. The fixed one-frame worker hand-off was 2.314× the direct median cost, so production listener integration remains deferred pending a measured multi-session batch benefit.
- [x] Audit and implement only one deterministic data-backed static-creature lifecycle slice, retaining full monster AI, loot, formulas, scripts, and NPC behavior as deferred. The only current typed behavior is already-supported interval-gated same-tile reactivation; typed spawn-zone, spectator, spawn-flag, rate-cap, and event-hook contracts are absent, so no new lifecycle behavior was invented. See `docs/protocol-evidence/static-creature-lifecycle-audit.md`.
- [x] Audit and implement only a parser-verified native 740 target and follow cleanup when an authoritative player-death transition invalidates the selected player, retaining generic PvP, death, loss, effects, and broader creature-lifecycle semantics as deferred. The central typed death transition now clears all authoritative target/follow references to the dead player; cross-session native `ClearTarget` delivery remains deferred pending session-ownership routing. See `docs/protocol-evidence/dead-player-interaction-cleanup.md`.
- [x] Audit and implement only a parser-verified native 740 `ClearTarget` delivery to the affected session after player-death target invalidation, retaining generic PvP, death presentation, cross-session effects, and broader lifecycle semantics as deferred. The lethal attacker’s current session now receives exactly one classic zero-payload `0xA3` frame after its selected target dies; cross-session notification remains deferred. See `docs/protocol-evidence/dead-player-interaction-cleanup.md`.
- [x] Audit persistent native 740 outfit state and implement only a validated persisted outfit change if current parser, authoritative-state, and SQLite contracts support it. Schema-v11 storage, validated range checks, relog hydration, self-update, and peer propagation already implement this bounded path; the remaining requirement is real unmodified OTCv8 confirmation. See `docs/protocol-evidence/native-outfit-persistence-audit.md`.
- [x] Audit native player mana, level, magic-level, and progression presentation against persisted stats and vocation rules; implement only a bounded correction if an independently tested inconsistency remains. Existing login hydration and vital-epoch refresh paths already source these fields from one persisted authoritative state; no current reproducible inconsistency remains. See `docs/protocol-evidence/native-progression-mana-audit.md`.
- [x] Add a reproducible multi-session native viewport-preparation benchmark comparing concurrent direct encoding with bounded worker preparation, preserving immutable inputs, byte-identical frames, raw samples, and no performance claim until measured. Three direct concurrent streams remained faster than three bounded workers by a 1.271× median ratio; live listener integration remains deferred pending a realistic end-to-end benefit.
- [x] Add a GitHub continuous-integration workflow that runs the Rust workspace tests, strict clippy, formatting check, capability-matrix JSON validation, and whitespace validation on supported changes.
- [x] Audit and implement only deterministic automatic reactivation of existing static creatures through the native heartbeat when current interval, map, and occupancy contracts support it; retain full spawn zones, AI, loot, scripts, and NPC behavior as deferred. The native heartbeat already advances the authoritative interval-gated reactivation path with player/static occupancy deferral, so no duplicate scheduler was added. See `docs/protocol-evidence/native-static-respawn-audit.md`.
- [x] Audit and implement only one deterministic automatic static-creature pursuit step through the native heartbeat if existing target, map, occupancy, and visibility contracts support it; retain general monster AI, pathfinding, combat, loot, scripts, and NPC behavior as deferred. `staticCreatureTargetPursuitRange = 0` preserves disabled behavior; values 1–8 enable exactly one existing nearest-living-player cardinal step per heartbeat. See `docs/protocol-evidence/native-static-pursuit.md`.
- [x] Audit and implement only one deterministic persisted native 740 map-action slice using existing item-use validation and authoritative map state; retain generic actions, scripts, doors, levers, switches, and full TFS compatibility as deferred. Existing imported teleport and read-only-text actions already cover the only safe typed metadata; action IDs alone have no execution contract. See `docs/protocol-evidence/native-map-action-audit.md`.
- [x] Refresh catalog-validated native 740 equipped-slot records after authoritative equipment changes, while retaining generic inventory movement, map-ground transfers, capacity, and container behavior as deferred. The listener already observes `equipment_epoch` and emits deterministic `SetInventory`/`DeleteInventory` deltas from the catalog-mapped authoritative snapshot.
- [x] Audit and implement one client-visible native 740 temple-respawn transition only when parser evidence and the persisted authoritative lifecycle state prove the packet sequence; retain automatic respawn timing and unverified client behavior as deferred. Stock OTCv8 relogs from its death dialog; FE already respawns a dead persisted player at the validated temple before the next native map bootstrap and atomically saves the position, restored vitals, and cleared death state.
- [x] Audit and implement one profile-explicit equipment-derived native melee mitigation slice only if imported item metadata and existing declarative contracts define a deterministic formula; retain TFS armor/shielding/weapon parity as deferred. The typed declarative selected-player weapon path now recalculates a capped profile-neutral physical reduction from validated positive `items.xml` armor in head, neck, armor, legs, feet, and ring slots. See `docs/protocol-evidence/native-equipment-armor-mitigation.md`.
- [x] Audit and implement one bounded static-creature physical mitigation bridge using the existing equipped armor-only reduction only when the authoritative static attack contract supports it; retain creature formulas, shielding, and TFS combat parity as deferred. The existing physical reduction now runs before static attack damage and lethal-state evaluation.
- [x] Audit and implement one bounded vocation armor-multiplier bridge for the existing native armor-only mitigation path only if parsed legacy vocation metadata and deterministic fixed-point rules provide direct evidence; retain shielding, random armor, and TFS formula parity as deferred. The validated `armor` multiplier now scales the capped six-slot armor sum through deterministic truncating thousandths with a safe `1.000` default.
- [x] Audit and route the native fixed selected-player melee fallback through the existing typed physical combat event only if heartbeat timing proves safe; retain generic TFS weapon timing and formula parity as deferred. The existing fallback now uses a one-tick adjacent physical event, so it applies armor reduction and rejects duplicate same-tick damage. See `docs/protocol-evidence/native-fixed-selected-melee.md`.
- [x] Audit and implement one bounded native 740 LookMap equipment-slot inspection path only if the existing classic inventory position and catalog validation contract supports it; retain container, ground, and full item-description behavior as deferred. The exact caller-owned fixed-slot form returns the existing status text with only slot code, server ID, and count. See `docs/protocol-evidence/native-equipment-look.md`.
- [x] Audit and implement one bounded native 740 LookMap owned top-level container-item inspection path only if the classic flagged container position and current ownership contract support it; retain nesting, generic descriptions, and container mutation as deferred. The exact current open non-nested container view returns the existing status text with only container ID, server ID, and count. See `docs/protocol-evidence/native-equipment-look.md`.
- [x] Audit and implement one deterministic native follow movement slice only if the current follow intent, authoritative movement, map, occupancy, and heartbeat contracts support it; retain pathfinding, formation logic, combat automation, and full TFS follow parity as deferred. Each current living player follow target permits one direct cardinal distance-reducing step per heartbeat with existing map and occupancy guards. See `docs/protocol-evidence/native-player-follow.md`.
- [x] Audit and implement one native 740 client-visible condition-status record only if the local OTCv8 parser and existing authoritative condition schedules define an exact safe wire contract; retain broader condition effects and unverified packet behavior as deferred. The local source exposes only newer feature-gated creature icons, not a verified classic player-condition record; no packet was added. See `docs/protocol-evidence/native-condition-status-audit.md`.
- [x] Audit and implement one bounded mapped stack transfer between two distinct caller-owned top-level native 740 containers only if existing authoritative container, merge, capacity, and ThrowItem contracts support it; retain nested containers, ground transfers, capacity systems, and generic inventory behavior as deferred. The new primitive atomically splits and merges/append stacks, persists both containers, and advances the existing refresh epoch. See `docs/protocol-evidence/native-container-to-container-transfer.md`.
- [x] Enforce the current-open native session container-view boundary for top-level container-to-container transfers, rejecting closed source or target windows before any authoritative mutation.
- [x] Audit and implement one deterministic capacity guard for native equipment and top-level-container transfers only if imported item-weight metadata and current authoritative player capacity define a complete safe contract; retain recursive weight, generic stackability, and TFS capacity parity as deferred. TFS-style weight exists upstream but is not retained in FE's bounded item catalog, so no incomplete guard was added. See `docs/protocol-evidence/native-capacity-audit.md`.
- [x] Audit and implement one bounded native 740 UseItem open-container action only for an exact caller-owned mapped top-level container root if current identity and packet contracts support it; retain generic actions, nesting, and script semantics as deferred. UseItem is map-only and FE lacks an unambiguous root location identity, so no unsafe open action was added. See `docs/protocol-evidence/native-useitem-container-open-audit.md`.
- [x] Re-audit and implement native 740 public chat delivery only when local OTCv8 parser definitions prove an exact outbound message-mode and record layout; retain unverified chat modes and broader social channels as deferred. The local `version >= 760` branch applies to 740 and maps `Say` to mode `1`; FE now sends one parser-backed `0xAA` name/mode/position/text record. See `docs/protocol-evidence/native-740-public-chat-reaudit.md`.
- [x] Audit and implement one parser-verified native 740 target cleanup when a selected player becomes authoritatively dead only if existing death, interaction, and ClearTarget ownership contracts support it; retain broader PvP and lifecycle semantics as deferred. The existing helper correctly clears only the defeating attacker; cross-session target ownership remains unmodeled, so no unsafe broadcast cleanup was added. See `docs/protocol-evidence/native-dead-player-target-cleanup-audit.md`.
- [x] Retain validated optional unsigned legacy `items.xml` weight metadata by authoritative server ID for future capacity work, without adding an incomplete capacity guard, recursive weight calculation, or transfer policy. Focused parser regressions cover nested and inline attributes, catalog export, and invalid values.
- [x] Retain validated legacy `items.xml` slotType labels by authoritative server ID for future equipment compatibility rules, without enabling slot placement, swaps, two-handed handling, or generic inventory policy. Focused parser regressions cover repeated nested labels, case-normalized inline values, typed catalog export, and invalid-label rejection.
- [x] Append one bounded validated imported legacy item weight to the existing native 740 map LookMap text only after exact authoritative item resolution, while retaining full descriptions, stack semantics, capacity, and all unverified inspection formats as deferred. The renderer calculates source-stack totals in hundredths of an ounce, preserves the bounded imported description, and is covered by direct and authenticated socket regressions.
- [x] Audit use of retained legacy `items.xml` slotType labels for native equipment placement before adding any transfer gate. TFS defaults each item to both hand bits and mutates an order-sensitive mask; FE retains an unordered set and lacks receiver/query, two-handed occupancy, cancellation, and move-event contracts, so no unsafe placement rule was added. See `docs/protocol-evidence/native-equipment-slot-placement-audit.md`.
- [x] Audit a bounded legacy TFS `spells.xml` declaration import before adding a compatibility adapter. The local reference base file is empty and behavior is Lua-driven; FE’s existing spell catalog is deliberately FE-owned and scriptless, so no misleading legacy runtime adapter was added. See `docs/protocol-evidence/legacy-spell-declaration-audit.md`.
- [x] Add one deterministic bounded cardinal detour step for static-creature pursuit after direct movement is blocked, retaining full TFS pathfinding, retry scheduling, movement speed, AI, combat, and script semantics as deferred. The opt-in pursuit policy performs at most one authoritative move and only searches to the configured target-range depth after direct routes fail; core regressions cover the direct-default block and deterministic detour, while established host visibility coverage remains green.
- [x] Audit a native 740 viewport-based selected-player target gate before adding one. The final classic renderer applies tile traversal, entity caps, and frame-budget clipping, while FE has no render-delivery token or typed PvP/query policy; no incomplete coordinate-only gate was added. See `docs/protocol-evidence/native-player-target-viewport-audit.md`.
- [x] Retain bounded legacy `items.xml` item names by authoritative server ID and append one `Name:` detail to exact native 740 map LookMap text before existing imported weight and description details. Parser, direct-render, and authenticated socket regressions cover the bounded metadata path; TFS article/plural forms and full descriptions remain deferred.
- [x] Reuse bounded imported legacy item-name and weight metadata for the existing exact native 740 fixed-equipment and top-level-container LookMap routes without changing mapping, slot, ownership, closed-window, or nesting validation. Direct inspection regressions cover non-stackable equipment and stack-aware container output.
- [x] Import one bounded legacy monster XML direct `melee` declaration per resolved monster definition, retaining interval and signed damage bounds for the bounded static combat bridge while leaving chance, ranged, area, status, spell, AI, and Lua attack behavior deferred. Focused catalog regression verifies `interval`, `min`, and negative `max` conversion.
- [x] Audit executing imported direct monster melee metadata through the existing static target attack heartbeat. The runtime has only a global fixed-damage pass and no persistent per-creature cooldown, beat conversion, or deterministic damage-selection contract, so imported data remains non-executing. See `docs/protocol-evidence/static-monster-melee-runtime-audit.md`.
- [x] Audit a player capacity foundation after importing legacy item weights. FE retains capacity vitals but has only flat non-recursive container windows rather than a complete carried-item graph, so no incomplete total or transfer gate was added. See `docs/protocol-evidence/player-capacity-foundation-audit.md`.
- [x] Retain bounded legacy monster `health now` metadata and derive only the initial static display health percentage from `now/max`, leaving damage, healing, reactivation, combat, and full monster health semantics unchanged. Resolved static-spawn regression verifies `15/20` initializes at 75%; impossible `now > max` is rejected during materialization.
- [x] Audit a bounded legacy registry-to-sandboxed-Lua callback adapter. Registry entries provide script references but no compatible typed event or argument contract for FE's explicit primitive callback dispatcher, so no unsafe automatic script registration was added. See `docs/protocol-evidence/lua-registry-callback-adapter-audit.md`.
- [x] Re-audit the native 740 empty Quest Log packet against the local OTCv8 parser. FE already emits the exact `0xF0` plus zero U16 quest count record and has a session-usability regression; no packet correction was needed, while real-client confirmation and quest semantics remain deferred. See `docs/protocol-evidence/native-empty-quest-log-reaudit.md`.
- [x] Refactor native ground-item ownership into one authoritative mutable world boundary before enabling ground-to-equipment or ground-to-container `ThrowItem` transfers. The current host renders from an immutable `Arc<WorldMap>` and must not mutate map preview data independently of `WorldState`.
- [x] Add a bounded synchronized native map-owner foundation with whole-tile replacement, revision tracking, and detached immutable render snapshots. It is not integrated with native session routing, player inventory, persistence, or shared-world lock order yet.
- [x] Add a deterministic source-map revision fingerprint over complete imported map content before introducing durable map-item identities or runtime-removal persistence.
- [x] Add a deterministic non-cryptographic source-map revision fingerprint over complete map metadata, ordered tiles, ordered nested item content, towns, houses, flags, and waypoints. Direct regressions prove cloned content is stable and a changed item count/children changes the revision; durable runtime identities, journal reconciliation, persistence, and client ground transfers remain deferred.
- [x] Define stable per-item source identities bound to the deterministic source-map revision before adding durable map-item journals or ground-inventory transfer routing.
- [x] Define stable top-level map-item source identities bound to the deterministic source-map revision, exact tile position, and ordered item index. Regressions distinguish duplicates, reject missing indexes, and invalidate identities after a source-content change; child identities, durable journals, reconciliation, persistence, and client ground transfers remain deferred.
- [x] Define a revision-bound durable map-item journal with explicit recovery and operator map-content change handling before atomic ground-inventory transfer routing.
- [x] Implement a lock-ordered composite authoritative map-item-to-player-inventory transfer that changes the synchronized map, shared player inventory, and revision-bound SQLite removal journal atomically; native ThrowItem routing and map delta delivery remain deferred.
- [x] Add a bounded per-static-creature melee cooldown foundation that converts imported direct-melee intervals upward to the authoritative one-second world clock, blocks attacks before each creature's due tick, and reports cooldown skips without changing global fixed-damage policy, client routing, or unsupported monster combat behavior. Core and shared-heartbeat regressions cover a 2,000 ms interval and no-metadata compatibility behavior.
- [x] Materialize normalized imported direct-melee damage ranges by static creature ID and use a deterministic inclusive ascending cycle at the existing bounded static target attack boundary. Imported ranges override only that creature's configured fixed fallback; regressions cover parser-to-core propagation and the `2, 3, 4, 2` cycle. TFS random selection, formulas, chance, mitigation parity, AI, scripts, effects, and client behavior remain deferred.
- [x] Persist and restore the bounded direct-melee damage-selection sequence through static creature runtime snapshots and SQLite schema v15. Core and host restart regressions prove a range resumes at its next deterministic value after a fresh shared world restores its durable record. Cooldown due ticks, target selection, formula state, generic monster persistence, AI, scripts, effects, and full TFS combat behavior remain deferred.
- [x] Persist and restore the bounded remaining direct-melee cooldown through static creature runtime snapshots and SQLite schema v16. FE stores only the nonnegative remaining authoritative ticks, validates them against each installed creature’s imported cooldown, and rebuilds the due tick relative to a fresh world. Core and host restart regressions prove an immediate restored attack remains suppressed until its remaining delay expires. Absolute world ticks, target selection, formula state, generic monster persistence, AI, scripts, effects, and full TFS combat behavior remain deferred.
- [x] Carry validated imported direct-melee interval metadata into the static spawn collection by stable creature ID before enabling any per-creature heartbeat cooldown behavior.
- [x] Replace the native listener’s immutable configured map handoff with one shared synchronized map owner and detached render snapshots, preserving existing native map rendering while preparing safe source-item pickup routing.
- [x] Recover revision-bound map-item removal journals into the live synchronized native map owner at startup, failing closed on an incompatible source map before a native game session is served.
- [x] Route one catalog-mapped classic 740 `ThrowItem` from a revision-bound top-level source-map item into one empty fixed equipment slot, with bounded inventory refresh and full-map refresh only; containers, stacks, capacity, and compact map deltas remain deferred.
- [x] Transfer one revision-bound complete source-map item atomically into one owned non-nested top-level container, then route the matching classic 740 `ThrowItem` request with container and full-map refreshes; stacks, capacity, nested containers, and compact deltas remain deferred.
- [x] Apply validated legacy `items.xml` slotType metadata to one bounded native 740 map-source-to-empty-equipment `ThrowItem` route. A known nonempty slot set must exactly admit the requested fixed slot; absent metadata preserves the prior route. Direct head/body/neck/backpack/leg/feet/ring/ammo/hand mappings are supported, while two-handed placement, generic equipment/container transfers, swaps, stack behavior, capacity, and full TFS slot masks remain deferred.
- [x] Apply that same validated legacy slotType check to the bounded native 740 owned top-level-container-to-empty-equipment `ThrowItem` route before its authoritative mutation. Occupied-slot exchanges and merges, map/container stack behavior, capacity, generic inventory rules, and full TFS slot-mask semantics remain deferred.
